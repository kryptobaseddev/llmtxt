---
title: ''
---

Type contracts exported by this package: interfaces, type aliases, and enums.

## AwarenessState

Presence state for a single agent.

| Property       | Type                  | Required | Description  |
| -------------- | --------------------- | -------- | ------------ |
| `agentId`      | `string`              | Yes      | agentId      |
| `section`      | `string`              | Yes      | section      |
| `cursorOffset` | `number \| undefined` | No       | cursorOffset |
| `lastSeen`     | `number`              | Yes      | lastSeen     |

## AwarenessEventType

Events emitted when awareness state changes.

```typescript
AwarenessEventType
```

## AwarenessEvent

| Property   | Type                     | Required | Description |
| ---------- | ------------------------ | -------- | ----------- |
| `type`     | `AwarenessEventType`     | Yes      | type        |
| `clientId` | `number`                 | Yes      | clientId    |
| `state`    | `AwarenessState \| null` | Yes      | state       |

## Unsubscribe

Unsubscribe function returned by onAwarenessChange.

```typescript
Unsubscribe
```

## CacheStats

Snapshot of cache performance statistics.

| Property  | Type     | Required | Description                                                      |
| --------- | -------- | -------- | ---------------------------------------------------------------- |
| `hits`    | `number` | Yes      | Total number of cache hits since last reset.                     |
| `misses`  | `number` | Yes      | Total number of cache misses since last reset.                   |
| `size`    | `number` | Yes      | Current number of live (non-expired) entries in the cache.       |
| `maxSize` | `number` | Yes      | Maximum number of entries the cache can hold.                    |
| `hitRate` | `number` | Yes      | Hit rate as a percentage (0-100), rounded to two decimal places. |

## LRUCacheOptions

Configuration options for constructing an `LRUCache` instance.

| Property  | Type                  | Required | Description                                                                                |
| --------- | --------------------- | -------- | ------------------------------------------------------------------------------------------ |
| `maxSize` | `number \| undefined` | No       | Maximum number of entries before the least-recently-used entry is evicted (default: 1000). |
| `ttl`     | `number \| undefined` | No       | Default time-to-live in milliseconds for new entries (default: 24 hours).                  |

## ClassificationResult

Result of `classifyContent`.  Mirrors `ClassificationResult` from `crates/llmtxt-core/src/classify/types.rs`.

| Property        | Type                                                                                                                                                                                                                                      | Required | Description                                                                                                                                                                                                                                                                                                                                         |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mimeType`      | `string`                                                                                                                                                                                                                                  | Yes      | IANA MIME type, e.g. `"application/pdf"`, `"text/markdown"`, `"image/png"`.                                                                                                                                                                                                                                                                         |
| `category`      | `"binary" \| "text" \| "structured" \| "unknown"`                                                                                                                                                                                         | Yes      | Coarse content category.                                                                                                                                                                                                                                                                                                                            |
| `format`        | `"unknown" \| "pdf" \| "png" \| "jpeg" \| "gif" \| "webp" \| "avif" \| "svg" \| "mp4" \| "webm" \| "mp3" \| "wav" \| "ogg" \| "zip" \| "markdown" \| "json" \| "javascript" \| "typescript" \| "python" \| "rust" \| "go" \| "plainText"` | Yes      | Specific content format.  Binary: `'pdf'` \| `'png'` \| `'jpeg'` \| `'gif'` \| `'webp'` \| `'avif'` \| `'svg'` \|         `'mp4'` \| `'webm'` \| `'mp3'` \| `'wav'` \| `'ogg'` \| `'zip'`  Text:   `'markdown'` \| `'json'` \| `'javascript'` \| `'typescript'` \| `'python'` \|         `'rust'` \| `'go'` \| `'plainText'`  Fallback: `'unknown'` |
| `confidence`    | `number`                                                                                                                                                                                                                                  | Yes      | Classification confidence in `[0.0, 1.0]`. See Rust spec for semantics.                                                                                                                                                                                                                                                                             |
| `isExtractable` | `boolean`                                                                                                                                                                                                                                 | Yes      | Whether useful text content can be extracted from this format. `true` for text formats and PDF; `false` for images, audio, video, zip.                                                                                                                                                                                                              |

## Section

A logical section identified within a document.

| Property     | Type                                                               | Required | Description                                                            |
| ------------ | ------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------- |
| `title`      | `string`                                                           | Yes      | Display title of the section (heading text, JSON key, or symbol name). |
| `depth`      | `number`                                                           | Yes      | Nesting depth (0-based). Headings use depth = level - 1.               |
| `startLine`  | `number`                                                           | Yes      | 1-based line number where the section begins.                          |
| `endLine`    | `number`                                                           | Yes      | 1-based line number where the section ends (inclusive).                |
| `tokenCount` | `number`                                                           | Yes      | Estimated token count for the section content.                         |
| `type`       | `"function" \| "heading" \| "json-key" \| "code-block" \| "class"` | Yes      | The structural type of the section.                                    |

## DocumentOverview

High-level structural overview of a document.

| Property     | Type                                                             | Required | Description                                                           |
| ------------ | ---------------------------------------------------------------- | -------- | --------------------------------------------------------------------- |
| `format`     | `"text" \| "markdown" \| "json" \| "code"`                       | Yes      | The detected document format.                                         |
| `lineCount`  | `number`                                                         | Yes      | Total number of lines in the document.                                |
| `tokenCount` | `number`                                                         | Yes      | Estimated total token count for the entire document.                  |
| `sections`   | `Section[]`                                                      | Yes      | Ordered list of sections found in the document.                       |
| `keys`       | `{ key: string; type: string; preview: string; }[] \| undefined` | No       | Top-level JSON keys with type info and preview (JSON documents only). |
| `toc`        | `{ title: string; depth: number; line: number; }[] \| undefined` | No       | Markdown table of contents entries (markdown documents only).         |

## SearchResult

A single match returned by `searchContent`.

| Property        | Type       | Required | Description                                                   |
| --------------- | ---------- | -------- | ------------------------------------------------------------- |
| `line`          | `number`   | Yes      | 1-based line number of the matching line.                     |
| `content`       | `string`   | Yes      | The full text of the matching line.                           |
| `contextBefore` | `string[]` | Yes      | Lines immediately preceding the match (up to `contextLines`). |
| `contextAfter`  | `string[]` | Yes      | Lines immediately following the match (up to `contextLines`). |

## LineRangeResult

Result of extracting a line range from a document via `getLineRange`.

| Property      | Type     | Required | Description                                                     |
| ------------- | -------- | -------- | --------------------------------------------------------------- |
| `startLine`   | `number` | Yes      | 1-based line number where the extracted range begins.           |
| `endLine`     | `number` | Yes      | 1-based line number where the extracted range ends (inclusive). |
| `content`     | `string` | Yes      | The extracted text content for the requested line range.        |
| `tokenCount`  | `number` | Yes      | Estimated token count for the extracted content.                |
| `totalLines`  | `number` | Yes      | Total number of lines in the full document.                     |
| `totalTokens` | `number` | Yes      | Estimated total token count for the full document.              |
| `tokensSaved` | `number` | Yes      | Number of tokens saved by extracting only this range.           |

## DiffResult

| Property        | Type     | Required | Description   |
| --------------- | -------- | -------- | ------------- |
| `addedLines`    | `number` | Yes      | addedLines    |
| `removedLines`  | `number` | Yes      | removedLines  |
| `addedTokens`   | `number` | Yes      | addedTokens   |
| `removedTokens` | `number` | Yes      | removedTokens |

## StructuredDiffLine

A single line in a structured diff with type annotation and line numbers.

| Property  | Type                                | Required | Description |
| --------- | ----------------------------------- | -------- | ----------- |
| `type`    | `"context" \| "added" \| "removed"` | Yes      | type        |
| `content` | `string`                            | Yes      | content     |
| `oldLine` | `number \| null`                    | Yes      | oldLine     |
| `newLine` | `number \| null`                    | Yes      | newLine     |

## StructuredDiffResult

Full structured diff result with interleaved lines and summary counts.

| Property           | Type                   | Required | Description      |
| ------------------ | ---------------------- | -------- | ---------------- |
| `lines`            | `StructuredDiffLine[]` | Yes      | lines            |
| `addedLineCount`   | `number`               | Yes      | addedLineCount   |
| `removedLineCount` | `number`               | Yes      | removedLineCount |
| `addedTokens`      | `number`               | Yes      | addedTokens      |
| `removedTokens`    | `number`               | Yes      | removedTokens    |

## MultiDiffVariant

A single version variant at a divergent line position.

| Property       | Type     | Required | Description  |
| -------------- | -------- | -------- | ------------ |
| `versionIndex` | `number` | Yes      | versionIndex |
| `content`      | `string` | Yes      | content      |

## MultiDiffLine

One line entry in a multi-way diff result.

| Property     | Type                                        | Required | Description                                                                                                                             |
| ------------ | ------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `lineNumber` | `number`                                    | Yes      | lineNumber                                                                                                                              |
| `type`       | `"consensus" \| "divergent" \| "insertion"` | Yes      | "consensus" when all versions agree, "divergent" when versions differ,  "insertion" when a version adds a line not present in the base. |
| `content`    | `string`                                    | Yes      | content                                                                                                                                 |
| `agreement`  | `number`                                    | Yes      | How many versions have `content` at this position.                                                                                      |
| `total`      | `number`                                    | Yes      | Total number of versions (including the base).                                                                                          |
| `variants`   | `MultiDiffVariant[]`                        | Yes      | Per-version contents when type is "divergent"; empty for "consensus".                                                                   |

## MultiDiffStats

Aggregate statistics for a multi-way diff.

| Property              | Type     | Required | Description         |
| --------------------- | -------- | -------- | ------------------- |
| `totalLines`          | `number` | Yes      | totalLines          |
| `consensusLines`      | `number` | Yes      | consensusLines      |
| `divergentLines`      | `number` | Yes      | divergentLines      |
| `consensusPercentage` | `number` | Yes      | consensusPercentage |

## MultiDiffResult

Full result of a multi-way diff.

| Property       | Type              | Required | Description  |
| -------------- | ----------------- | -------- | ------------ |
| `baseVersion`  | `number`          | Yes      | baseVersion  |
| `versionCount` | `number`          | Yes      | versionCount |
| `lines`        | `MultiDiffLine[]` | Yes      | lines        |
| `stats`        | `MultiDiffStats`  | Yes      | stats        |

## CherryPickProvenance

A single provenance entry in the cherry-pick merged output.

| Property      | Type                   | Required | Description |
| ------------- | ---------------------- | -------- | ----------- |
| `lineStart`   | `number`               | Yes      | lineStart   |
| `lineEnd`     | `number`               | Yes      | lineEnd     |
| `fromVersion` | `number`               | Yes      | fromVersion |
| `fillFrom`    | `boolean \| undefined` | No       | fillFrom    |

## CherryPickStats

Statistics for a cherry-pick merge operation.

| Property              | Type     | Required | Description         |
| --------------------- | -------- | -------- | ------------------- |
| `totalLines`          | `number` | Yes      | totalLines          |
| `sourcesUsed`         | `number` | Yes      | sourcesUsed         |
| `sectionsExtracted`   | `number` | Yes      | sectionsExtracted   |
| `lineRangesExtracted` | `number` | Yes      | lineRangesExtracted |

## CherryPickResult

Return value of a cherry-pick merge operation.

| Property     | Type                     | Required | Description |
| ------------ | ------------------------ | -------- | ----------- |
| `content`    | `string`                 | Yes      | content     |
| `provenance` | `CherryPickProvenance[]` | Yes      | provenance  |
| `stats`      | `CherryPickStats`        | Yes      | stats       |

## Conflict

A single conflict region from a 3-way merge.

| Property        | Type     | Required | Description                                                          |
| --------------- | -------- | -------- | -------------------------------------------------------------------- |
| `oursStart`     | `number` | Yes      | 1-based start line of the conflicting region in `ours`.              |
| `oursEnd`       | `number` | Yes      | 1-based end line of the conflicting region in `ours` (inclusive).    |
| `theirsStart`   | `number` | Yes      | 1-based start line of the conflicting region in `theirs`.            |
| `theirsEnd`     | `number` | Yes      | 1-based end line of the conflicting region in `theirs` (inclusive).  |
| `baseStart`     | `number` | Yes      | 1-based start line of the conflicting region in the common ancestor. |
| `baseEnd`       | `number` | Yes      | 1-based end line of the conflicting region in the common ancestor.   |
| `oursContent`   | `string` | Yes      | The conflicting text from `ours`.                                    |
| `theirsContent` | `string` | Yes      | The conflicting text from `theirs`.                                  |
| `baseContent`   | `string` | Yes      | The original text from the common ancestor.                          |

## MergeStats

Statistics for a 3-way merge operation.

| Property          | Type     | Required | Description                                                    |
| ----------------- | -------- | -------- | -------------------------------------------------------------- |
| `totalLines`      | `number` | Yes      | Total lines in the merged output (including conflict markers). |
| `autoMergedLines` | `number` | Yes      | Number of lines accepted without conflict.                     |
| `conflictCount`   | `number` | Yes      | Number of distinct conflict regions.                           |

## ThreeWayMergeResult

Full result of a 3-way merge operation.

| Property       | Type         | Required | Description                                                          |
| -------------- | ------------ | -------- | -------------------------------------------------------------------- |
| `merged`       | `string`     | Yes      | The merged document content, with conflict markers where applicable. |
| `hasConflicts` | `boolean`    | Yes      | `true` when at least one conflict could not be auto-merged.          |
| `conflicts`    | `Conflict[]` | Yes      | Details of each conflict region.                                     |
| `stats`        | `MergeStats` | Yes      | Summary statistics for the merge.                                    |

## SectionAlignment

How a section from version A maps to version B.

```typescript
SectionAlignment
```

## SectionSimilarity

Per-section similarity record produced by semantic diff.

| Property     | Type               | Required | Description |
| ------------ | ------------------ | -------- | ----------- |
| `sectionA`   | `string`           | Yes      | sectionA    |
| `sectionB`   | `string`           | Yes      | sectionB    |
| `similarity` | `number`           | Yes      | similarity  |
| `alignment`  | `SectionAlignment` | Yes      | alignment   |

## SemanticChange

A semantic change annotation for a matched/renamed section pair.

| Property      | Type     | Required | Description                                                |
| ------------- | -------- | -------- | ---------------------------------------------------------- |
| `changeType`  | `string` | Yes      | One of: "unchanged", "rephrased", "modified", "rewritten". |
| `section`     | `string` | Yes      | section                                                    |
| `similarity`  | `number` | Yes      | similarity                                                 |
| `description` | `string` | Yes      | description                                                |

## SemanticDiffResult

Full result of a semantic diff between two document versions.

| Property              | Type                  | Required | Description         |
| --------------------- | --------------------- | -------- | ------------------- |
| `overallSimilarity`   | `number`              | Yes      | overallSimilarity   |
| `sectionSimilarities` | `SectionSimilarity[]` | Yes      | sectionSimilarities |
| `semanticChanges`     | `SemanticChange[]`    | Yes      | semanticChanges     |

## ReviewCluster

A cluster of reviewers whose embeddings are mutually similar.

| Property        | Type       | Required | Description   |
| --------------- | ---------- | -------- | ------------- |
| `members`       | `string[]` | Yes      | members       |
| `avgSimilarity` | `number`   | Yes      | avgSimilarity |

## SemanticConsensusResult

Result of semantic consensus evaluation across a set of reviews.

| Property         | Type              | Required | Description    |
| ---------------- | ----------------- | -------- | -------------- |
| `consensus`      | `boolean`         | Yes      | consensus      |
| `agreementScore` | `number`          | Yes      | agreementScore |
| `clusters`       | `ReviewCluster[]` | Yes      | clusters       |
| `outliers`       | `string[]`        | Yes      | outliers       |

## GraphNode

A node in the knowledge graph.

| Property | Type     | Required | Description |
| -------- | -------- | -------- | ----------- |
| `id`     | `string` | Yes      | id          |
| `type`   | `string` | Yes      | type        |
| `label`  | `string` | Yes      | label       |
| `weight` | `number` | Yes      | weight      |

## GraphEdge

An edge in the knowledge graph.

| Property | Type     | Required | Description |
| -------- | -------- | -------- | ----------- |
| `source` | `string` | Yes      | source      |
| `target` | `string` | Yes      | target      |
| `type`   | `string` | Yes      | type        |
| `weight` | `number` | Yes      | weight      |

## GraphStats

Statistics for a knowledge graph.

| Property        | Type     | Required | Description   |
| --------------- | -------- | -------- | ------------- |
| `agentCount`    | `number` | Yes      | agentCount    |
| `topicCount`    | `number` | Yes      | topicCount    |
| `decisionCount` | `number` | Yes      | decisionCount |
| `edgeCount`     | `number` | Yes      | edgeCount     |

## KnowledgeGraph

A knowledge graph containing nodes, edges, and statistics.

| Property | Type          | Required | Description |
| -------- | ------------- | -------- | ----------- |
| `nodes`  | `GraphNode[]` | Yes      | nodes       |
| `edges`  | `GraphEdge[]` | Yes      | edges       |
| `stats`  | `GraphStats`  | Yes      | stats       |

## MessageInput

Input message for graph construction.

| Property      | Type                                                                            | Required | Description |
| ------------- | ------------------------------------------------------------------------------- | -------- | ----------- |
| `id`          | `string`                                                                        | Yes      | id          |
| `fromAgentId` | `string`                                                                        | Yes      | fromAgentId |
| `content`     | `string`                                                                        | Yes      | content     |
| `metadata`    | `{ mentions?: string[]; directives?: string[]; tags?: string[]; } \| undefined` | No       | metadata    |
| `createdAt`   | `string`                                                                        | Yes      | createdAt   |

## SimilarityRankResult

Result entry from rankBySimilarity.

| Property | Type     | Required | Description |
| -------- | -------- | -------- | ----------- |
| `index`  | `number` | Yes      | index       |
| `score`  | `number` | Yes      | score       |

## AuditAction

Action type for audit logging. Represents the type of operation that occurred.

```typescript
AuditAction
```

## ApiVersionInfo

Metadata for a specific API version.

| Property     | Type                  | Required | Description                                                 |
| ------------ | --------------------- | -------- | ----------------------------------------------------------- |
| `version`    | `number`              | Yes      | version                                                     |
| `deprecated` | `boolean`             | Yes      | deprecated                                                  |
| `sunset`     | `string \| undefined` | No       | ISO 8601 date on which this version will stop being served. |

## LinkType

Type for a cross-document link relationship.

```typescript
"references" | "depends_on" | "derived_from" | "supersedes" | "related"
```

## ContentFormat

Supported content formats.

```typescript
ContentFormat
```

## DocumentMode

Lifecycle state for collaborative documents.

```typescript
DocumentMode
```

## DocumentMeta

Metadata for a stored document.

| Property         | Type                        | Required | Description                                                    |
| ---------------- | --------------------------- | -------- | -------------------------------------------------------------- |
| `id`             | `string`                    | Yes      | id                                                             |
| `slug`           | `string`                    | Yes      | slug                                                           |
| `format`         | `ContentFormat`             | Yes      | format                                                         |
| `contentHash`    | `string`                    | Yes      | contentHash                                                    |
| `originalSize`   | `number`                    | Yes      | originalSize                                                   |
| `compressedSize` | `number`                    | Yes      | compressedSize                                                 |
| `tokenCount`     | `number`                    | Yes      | tokenCount                                                     |
| `createdAt`      | `number`                    | Yes      | createdAt                                                      |
| `expiresAt`      | `number \| null`            | Yes      | expiresAt                                                      |
| `accessCount`    | `number`                    | Yes      | accessCount                                                    |
| `lastAccessedAt` | `number \| null`            | Yes      | lastAccessedAt                                                 |
| `mode`           | `DocumentMode \| undefined` | No       | Lifecycle state (collaborative documents).                     |
| `versionCount`   | `number \| undefined`       | No       | Total number of versions.                                      |
| `currentVersion` | `number \| undefined`       | No       | Current version number.                                        |
| `storageKey`     | `string \| undefined`       | No       | Object storage key when content lives in S3 instead of inline. |

## VersionMeta

Metadata for a single document version.

| Property        | Type                  | Required | Description   |
| --------------- | --------------------- | -------- | ------------- |
| `id`            | `string`              | Yes      | id            |
| `documentId`    | `string`              | Yes      | documentId    |
| `versionNumber` | `number`              | Yes      | versionNumber |
| `contentHash`   | `string`              | Yes      | contentHash   |
| `tokenCount`    | `number`              | Yes      | tokenCount    |
| `createdAt`     | `number`              | Yes      | createdAt     |
| `createdBy`     | `string \| undefined` | No       | createdBy     |
| `changelog`     | `string \| undefined` | No       | changelog     |

## VersionSummary

Summary of a version for listing (no content).

| Property        | Type                  | Required | Description   |
| --------------- | --------------------- | -------- | ------------- |
| `versionNumber` | `number`              | Yes      | versionNumber |
| `tokenCount`    | `number`              | Yes      | tokenCount    |
| `createdAt`     | `number`              | Yes      | createdAt     |
| `createdBy`     | `string \| undefined` | No       | createdBy     |
| `changelog`     | `string \| undefined` | No       | changelog     |

## VersionDiff

Result of comparing two versions.

| Property        | Type     | Required | Description   |
| --------------- | -------- | -------- | ------------- |
| `documentId`    | `string` | Yes      | documentId    |
| `fromVersion`   | `number` | Yes      | fromVersion   |
| `toVersion`     | `number` | Yes      | toVersion     |
| `addedTokens`   | `number` | Yes      | addedTokens   |
| `removedTokens` | `number` | Yes      | removedTokens |
| `addedLines`    | `number` | Yes      | addedLines    |
| `removedLines`  | `number` | Yes      | removedLines  |

## LlmtxtRef

Reference to an llmtxt document shared in a message.

| Property     | Type            | Required | Description |
| ------------ | --------------- | -------- | ----------- |
| `slug`       | `string`        | Yes      | slug        |
| `url`        | `string`        | Yes      | url         |
| `format`     | `ContentFormat` | Yes      | format      |
| `tokenCount` | `number`        | Yes      | tokenCount  |
| `preview`    | `string`        | Yes      | preview     |

## AttachmentOptions

Options for creating an attachment via the bridge.

| Property         | Type                         | Required | Description    |
| ---------------- | ---------------------------- | -------- | -------------- |
| `content`        | `string`                     | Yes      | content        |
| `format`         | `ContentFormat \| undefined` | No       | format         |
| `conversationId` | `string`                     | Yes      | conversationId |
| `fromAgentId`    | `string`                     | Yes      | fromAgentId    |
| `expiresInMs`    | `number \| undefined`        | No       | expiresInMs    |

## AttachmentAccessMode

Attachment fetch mode supported by the bridge/API layer.

```typescript
AttachmentAccessMode
```

## AttachmentSharingMode

Share state persisted by the API layer for an attachment.

```typescript
AttachmentSharingMode
```

## AttachmentReshareOptions

Options for re-sharing an existing attachment.

| Property    | Type                                 | Required | Description |
| ----------- | ------------------------------------ | -------- | ----------- |
| `expiresIn` | `number \| undefined`                | No       | expiresIn   |
| `mode`      | `AttachmentSharingMode \| undefined` | No       | mode        |

## AttachmentVersionOptions

Options for appending a version to an existing attachment slug.

| Property      | Type                  | Required | Description |
| ------------- | --------------------- | -------- | ----------- |
| `baseVersion` | `number \| undefined` | No       | baseVersion |
| `changelog`   | `string \| undefined` | No       | changelog   |

## Permission

Fine-grained permission on a document. Canonical definition; mirrors crates/llmtxt-core::rbac::Permission.

```typescript
Permission
```

## DocumentRole

Role a user holds on a specific document. Canonical definition; mirrors crates/llmtxt-core::rbac::DocumentRole.

```typescript
DocumentRole
```

## OrgRole

Role a user holds within an organisation. Canonical definition; mirrors crates/llmtxt-core::rbac::OrgRole.

```typescript
OrgRole
```

## DocumentEventType

Discriminant for document lifecycle events emitted by the event bus.  Consumers should use this type when subscribing to the bus or when filtering events in webhook handlers.

```typescript
DocumentEventType
```

## DocumentEvent

Payload for a document lifecycle event.  Emitted by the in-process event bus after a successful database write. Consumers include WebSocket/SSE streams and webhook delivery workers.

| Property     | Type                      | Required | Description                                                            |
| ------------ | ------------------------- | -------- | ---------------------------------------------------------------------- |
| `type`       | `DocumentEventType`       | Yes      | Discriminant — consumers can switch on this.                           |
| `slug`       | `string`                  | Yes      | Short URL slug of the affected document.                               |
| `documentId` | `string`                  | Yes      | Opaque document primary key.                                           |
| `timestamp`  | `number`                  | Yes      | Unix timestamp in milliseconds.                                        |
| `actor`      | `string`                  | Yes      | userId or agentId that triggered the event. 'system' for auto-actions. |
| `data`       | `Record<string, unknown>` | Yes      | Event-specific supplemental data.                                      |

## LlmtxtClientConfig

| Property  | Type     | Required | Description |
| --------- | -------- | -------- | ----------- |
| `apiBase` | `string` | Yes      | apiBase     |
| `apiKey`  | `string` | Yes      | apiKey      |
| `agentId` | `string` | Yes      | agentId     |

## UploadResult

| Property           | Type     | Required | Description      |
| ------------------ | -------- | -------- | ---------------- |
| `slug`             | `string` | Yes      | slug             |
| `contentHash`      | `string` | Yes      | contentHash      |
| `originalSize`     | `number` | Yes      | originalSize     |
| `compressedSize`   | `number` | Yes      | compressedSize   |
| `compressionRatio` | `number` | Yes      | compressionRatio |
| `tokens`           | `number` | Yes      | tokens           |
| `signedUrl`        | `string` | Yes      | signedUrl        |
| `expiresAt`        | `number` | Yes      | expiresAt        |

## FetchResult

| Property       | Type                  | Required | Description  |
| -------------- | --------------------- | -------- | ------------ |
| `slug`         | `string`              | Yes      | slug         |
| `content`      | `string`              | Yes      | content      |
| `format`       | `string`              | Yes      | format       |
| `title`        | `string \| undefined` | No       | title        |
| `contentHash`  | `string`              | Yes      | contentHash  |
| `originalSize` | `number`              | Yes      | originalSize |
| `tokens`       | `number`              | Yes      | tokens       |

## ReshareResult

| Property    | Type                          | Required | Description |
| ----------- | ----------------------------- | -------- | ----------- |
| `slug`      | `string`                      | Yes      | slug        |
| `mode`      | `AttachmentSharingMode`       | Yes      | mode        |
| `signedUrl` | `string \| undefined`         | No       | signedUrl   |
| `expiresAt` | `number \| null \| undefined` | No       | expiresAt   |

## ResignResult

Backward-compatible alias. Prefer `ReshareResult`.

```typescript
ReshareResult
```

## AttachmentVersionResult

| Property        | Type                  | Required | Description   |
| --------------- | --------------------- | -------- | ------------- |
| `slug`          | `string`              | Yes      | slug          |
| `versionNumber` | `number`              | Yes      | versionNumber |
| `patchText`     | `string \| undefined` | No       | patchText     |
| `contentHash`   | `string \| undefined` | No       | contentHash   |
| `createdAt`     | `string \| undefined` | No       | createdAt     |
| `createdBy`     | `string \| undefined` | No       | createdBy     |
| `changelog`     | `string \| undefined` | No       | changelog     |

## SectionDelta

A delta event emitted when the CRDT state for a section changes.

| Property      | Type                          | Required | Description                                                                                                                                                |
| ------------- | ----------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `slug`        | `string`                      | Yes      | Document slug.                                                                                                                                             |
| `sectionId`   | `string`                      | Yes      | Section identifier.                                                                                                                                        |
| `text`        | `string`                      | Yes      | Current plain-text content of the section after applying the delta.                                                                                        |
| `updateBytes` | `Uint8Array<ArrayBufferLike>` | Yes      | Raw Loro update bytes that caused this delta (Uint8Array). These are Loro binary format bytes — bitwise incompatible with the previous Yrs lib0 v1 format. |
| `receivedAt`  | `number`                      | Yes      | Wall clock timestamp (ms since epoch) when the update was received.                                                                                        |

## Unsubscribe

Function to call to unsubscribe and close the WebSocket connection.

```typescript
Unsubscribe
```

## SubscribeSectionOptions

Options for `subscribeSection`.

| Property      | Type                                           | Required | Description                                                                                                           |
| ------------- | ---------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------- |
| `baseUrl`     | `string \| undefined`                          | No       | Base URL of the llmtxt API. Defaults to `https://api.llmtxt.my`.                                                      |
| `token`       | `string \| undefined`                          | No       | Bearer token for authentication. Pass your API key here (llmtxt\_... format).                                         |
| `onError`     | `((err: Event) => void) \| undefined`          | No       | Called when the WebSocket encounters an error.                                                                        |
| `onAwareness` | `((payload: Uint8Array) => void) \| undefined` | No       | Called when an AwarenessRelay (0x04) message is received. The payload is the raw awareness bytes relayed from a peer. |

## EmbeddingProvider

Standard embedding provider interface — matches apps/backend/src/utils/embeddings.ts. Implemented by both the local ONNX provider and the TF-IDF fallback.

| Property     | Type                                       | Required | Description |
| ------------ | ------------------------------------------ | -------- | ----------- |
| `embed`      | `(texts: string[]) => Promise<number[][]>` | Yes      | embed       |
| `dimensions` | `number`                                   | Yes      | dimensions  |
| `model`      | `string`                                   | Yes      | model       |
| `provider`   | `string`                                   | Yes      | provider    |

## SignatureHeaders

HTTP headers produced by `AgentIdentity.buildSignatureHeaders` / `signRequest`.

| Property              | Type     | Required | Description                                                               |
| --------------------- | -------- | -------- | ------------------------------------------------------------------------- |
| `'X-Agent-Pubkey-Id'` | `string` | Yes      | Agent identifier used to look up the registered public key on the server. |
| `'X-Agent-Signature'` | `string` | Yes      | Hex-encoded 64-byte Ed25519 signature over the canonical payload.         |
| `'X-Agent-Nonce'`     | `string` | Yes      | Hex-encoded random nonce (16 bytes = 32 hex chars).                       |
| `'X-Agent-Timestamp'` | `string` | Yes      | Milliseconds since epoch as a decimal string.                             |

## CanonicalPayloadOptions

Options for `buildCanonicalPayload`.  All fields map directly to the Rust `canonical_payload()` parameters in `crates/llmtxt-core/src/identity.rs`.

| Property      | Type     | Required | Description                                                 |
| ------------- | -------- | -------- | ----------------------------------------------------------- |
| `method`      | `string` | Yes      | HTTP method, uppercase (e.g. "PUT").                        |
| `path`        | `string` | Yes      | Path and query string (e.g. "/api/v1/documents/abc").       |
| `timestampMs` | `number` | Yes      | Milliseconds since epoch.                                   |
| `agentId`     | `string` | Yes      | Agent identifier registered on the server.                  |
| `nonceHex`    | `string` | Yes      | Hex-encoded nonce (≥ 16 bytes = ≥ 32 hex chars).            |
| `bodyHashHex` | `string` | Yes      | Lowercase hex SHA-256 of the request body bytes (64 chars). |

## PredefinedSchemaName

Union of all registered predefined schema name strings.

```typescript
"prompt-v1"
```

## JsonFormat

Inferred TypeScript type for any valid JSON value (object, array, primitive).

```typescript
string | number | boolean | Record<string, unknown> | unknown[] | null
```

## TextFormat

Inferred TypeScript type for plain text content.

```typescript
string
```

## MarkdownFormat

Inferred TypeScript type for markdown content (string alias).

```typescript
string
```

## PromptV1

Inferred TypeScript type for the standard LLM prompt format.

```typescript
{ messages: { role: "system" | "user" | "assistant"; content: string; }[]; system?: string | undefined; temperature?: number | undefined; max_tokens?: number | undefined; }
```

## PromptMessage

Inferred TypeScript type for a single prompt message with role and content.

```typescript
{ role: "system" | "user" | "assistant"; content: string; }
```

## CompressRequest

Inferred TypeScript type for content compression request payloads.

```typescript
{ content: string; format: "text" | "markdown" | "json"; schema?: string | undefined; metadata?: Record<string, unknown> | undefined; }
```

## DecompressRequest

Inferred TypeScript type for content decompression request payloads.

```typescript
{ slug: string; }
```

## SearchRequest

Inferred TypeScript type for content search request payloads.

```typescript
{ query: string; slugs: string[]; }
```

## ValidationResult

Outcome of a content validation operation.

| Property  | Type                                          | Required | Description                                                        |
| --------- | --------------------------------------------- | -------- | ------------------------------------------------------------------ |
| `success` | `boolean`                                     | Yes      | Whether the validation passed without errors.                      |
| `data`    | `T \| undefined`                              | No       | The parsed/validated data, present only when `success` is `true`.  |
| `errors`  | `ValidationError[] \| undefined`              | No       | List of validation errors, present only when `success` is `false`. |
| `format`  | `"text" \| "markdown" \| "json" \| undefined` | No       | The detected or requested content format, when determinable.       |

## ValidationError

A single validation error with location, message, and error code.

| Property  | Type     | Required | Description                                                                     |
| --------- | -------- | -------- | ------------------------------------------------------------------------------- |
| `path`    | `string` | Yes      | Dot-delimited path to the field that failed validation (empty string for root). |
| `message` | `string` | Yes      | Human-readable description of the validation failure.                           |
| `code`    | `string` | Yes      | Machine-readable error code (e.g. `"invalid_json"`, `"unknown_schema"`).        |

## ValidateContentOptions

Options for content validation.

| Property       | Type                   | Required | Description                                                               |
| -------------- | ---------------------- | -------- | ------------------------------------------------------------------------- |
| `schemaName`   | `string \| undefined`  | No       | Optional predefined schema name for JSON validation.                      |
| `maxBytes`     | `number \| undefined`  | No       | Maximum content size in bytes. Set to 0 to disable. Default: 5 MB.        |
| `maxLineBytes` | `number \| undefined`  | No       | Maximum line length in bytes. Set to 0 to disable. Default: 64 KB.        |
| `rejectBinary` | `boolean \| undefined` | No       | Reject content with binary control characters (0x00-0x08). Default: true. |

## SignedUrlParams

Parameters that uniquely identify a signed URL access grant.

| Property         | Type     | Required | Description    |
| ---------------- | -------- | -------- | -------------- |
| `slug`           | `string` | Yes      | slug           |
| `agentId`        | `string` | Yes      | agentId        |
| `conversationId` | `string` | Yes      | conversationId |
| `expiresAt`      | `number` | Yes      | expiresAt      |

## SignedUrlConfig

Configuration for generating and verifying signed URLs.

| Property          | Type                  | Required | Description                                                   |
| ----------------- | --------------------- | -------- | ------------------------------------------------------------- |
| `secret`          | `string`              | Yes      | secret                                                        |
| `baseUrl`         | `string`              | Yes      | baseUrl                                                       |
| `pathPrefix`      | `string \| undefined` | No       | Optional path prefix like `/attachments`. Default: root path. |
| `signatureLength` | `number \| undefined` | No       | Signature length in hex chars. Default: 16.                   |

## VerifyResult

Outcome of verifying a signed URL.

| Property | Type                                                                | Required | Description |
| -------- | ------------------------------------------------------------------- | -------- | ----------- |
| `valid`  | `boolean`                                                           | Yes      | valid       |
| `reason` | `"missing_params" \| "expired" \| "invalid_signature" \| undefined` | No       | reason      |
| `params` | `SignedUrlParams \| undefined`                                      | No       | params      |

## OrgSignedUrlParams

Parameters for org-scoped signed URLs (Phase 5 enterprise). Extends conversation-scoped params with an organization ID.

| Property | Type     | Required | Description |
| -------- | -------- | -------- | ----------- |
| `orgId`  | `string` | Yes      | orgId       |

## SnapshotMeta

| Property           | Type     | Required | Description      |
| ------------------ | -------- | -------- | ---------------- |
| `sessionId`        | `string` | Yes      | sessionId        |
| `agentId`          | `string` | Yes      | agentId          |
| `createdAt`        | `string` | Yes      | createdAt        |
| `originalSize`     | `number` | Yes      | originalSize     |
| `compressedSize`   | `number` | Yes      | compressedSize   |
| `compressionRatio` | `number` | Yes      | compressionRatio |
| `tokens`           | `number` | Yes      | tokens           |
| `contentHash`      | `string` | Yes      | contentHash      |

## CompressedSnapshot

| Property | Type                      | Required | Description |
| -------- | ------------------------- | -------- | ----------- |
| `meta`   | `SnapshotMeta`            | Yes      | meta        |
| `data`   | `Buffer<ArrayBufferLike>` | Yes      | data        |

## SnapshotOptions

| Property              | Type                   | Required | Description                                                 |
| --------------------- | ---------------------- | -------- | ----------------------------------------------------------- |
| `includeDecisions`    | `boolean \| undefined` | No       | Include full decision log (default: true)                   |
| `includeObservations` | `boolean \| undefined` | No       | Include brain observations (default: true)                  |
| `maxSize`             | `number \| undefined`  | No       | Max content size before compression in bytes (default: 5MB) |

## DocumentEventLogEntry

A single event row from the document event log.

| Property     | Type                      | Required | Description                                                                      |
| ------------ | ------------------------- | -------- | -------------------------------------------------------------------------------- |
| `id`         | `string`                  | Yes      | id                                                                               |
| `seq`        | `string`                  | Yes      | Monotonically increasing per-document sequence number (as string — bigint-safe). |
| `event_type` | `string`                  | Yes      | event\_type                                                                      |
| `actor_id`   | `string`                  | Yes      | actor\_id                                                                        |
| `payload`    | `Record<string, unknown>` | Yes      | payload                                                                          |
| `created_at` | `string`                  | Yes      | created\_at                                                                      |

## WatchDocumentOptions

| Property        | Type                            | Required | Description                                                                                            |
| --------------- | ------------------------------- | -------- | ------------------------------------------------------------------------------------------------------ |
| `fromSeq`       | `string \| number \| undefined` | No       | Resume from this sequence number (exclusive). If unset, starts from the beginning.                     |
| `apiKey`        | `string \| undefined`           | No       | Bearer token or API key for authenticated requests. Passed as `Authorization: Bearer <apiKey>` header. |
| `maxReconnects` | `number \| undefined`           | No       | Maximum reconnect attempts within the circuit-breaker window (60s). Defaults to 5.                     |
| `signal`        | `AbortSignal \| undefined`      | No       | AbortSignal to cancel the stream externally.                                                           |

## DocumentState

Lifecycle state of a collaborative document.

```typescript
DocumentState
```

## StateTransition

Record of a lifecycle state change.

| Property    | Type                  | Required | Description                                        |
| ----------- | --------------------- | -------- | -------------------------------------------------- |
| `from`      | `DocumentState`       | Yes      | State before the transition.                       |
| `to`        | `DocumentState`       | Yes      | State after the transition.                        |
| `changedBy` | `string`              | Yes      | Agent that initiated the transition.               |
| `changedAt` | `number`              | Yes      | Timestamp of the transition (ms since epoch).      |
| `reason`    | `string \| undefined` | No       | Human-readable reason for the transition.          |
| `atVersion` | `number`              | Yes      | Document version number at the time of transition. |

## TransitionResult

Result of attempting a state transition.

| Property         | Type                       | Required | Description                                                     |
| ---------------- | -------------------------- | -------- | --------------------------------------------------------------- |
| `valid`          | `boolean`                  | Yes      | Whether the transition is allowed.                              |
| `reason`         | `string \| undefined`      | No       | Human-readable explanation when invalid.                        |
| `allowedTargets` | `readonly DocumentState[]` | Yes      | The allowed targets from the current state (for error context). |

## VersionEntry

A single version entry in a document's patch stack.

| Property        | Type     | Required | Description                                                      |
| --------------- | -------- | -------- | ---------------------------------------------------------------- |
| `versionNumber` | `number` | Yes      | Sequential version number (1-based).                             |
| `patchText`     | `string` | Yes      | Unified diff patch text.                                         |
| `createdBy`     | `string` | Yes      | Agent that authored this version.                                |
| `changelog`     | `string` | Yes      | One-line description of the change.                              |
| `contentHash`   | `string` | Yes      | SHA-256 hash of the resulting content after applying this patch. |
| `createdAt`     | `number` | Yes      | Timestamp of creation (ms since epoch).                          |

## ReconstructionResult

Result of reconstructing a document at a specific version.

| Property         | Type     | Required | Description                                      |
| ---------------- | -------- | -------- | ------------------------------------------------ |
| `content`        | `string` | Yes      | The document content at the requested version.   |
| `version`        | `number` | Yes      | The version number that was reconstructed.       |
| `patchesApplied` | `number` | Yes      | Number of patches applied to reach this version. |
| `contentHash`    | `string` | Yes      | SHA-256 hash of the reconstructed content.       |
| `tokenCount`     | `number` | Yes      | Token count of the reconstructed content.        |

## PatchValidationResult

Result of validating whether a patch applies cleanly.

| Property        | Type                  | Required | Description                                         |
| --------------- | --------------------- | -------- | --------------------------------------------------- |
| `applies`       | `boolean`             | Yes      | Whether the patch can be applied without conflicts. |
| `error`         | `string \| undefined` | No       | Error message if the patch does not apply.          |
| `resultContent` | `string \| undefined` | No       | The content that would result if the patch applies. |

## VersionDiffSummary

Summary of changes between two versions.

| Property        | Type     | Required | Description                      |
| --------------- | -------- | -------- | -------------------------------- |
| `fromVersion`   | `number` | Yes      | Source version number.           |
| `toVersion`     | `number` | Yes      | Target version number.           |
| `addedLines`    | `number` | Yes      | Lines added between versions.    |
| `removedLines`  | `number` | Yes      | Lines removed between versions.  |
| `addedTokens`   | `number` | Yes      | Tokens added between versions.   |
| `removedTokens` | `number` | Yes      | Tokens removed between versions. |
| `patchText`     | `string` | Yes      | Unified diff text.               |

## ApprovalStatus

Status of an individual review.

```typescript
ApprovalStatus
```

## Review

A single review from an agent.

| Property     | Type                  | Required | Description                                                               |
| ------------ | --------------------- | -------- | ------------------------------------------------------------------------- |
| `reviewerId` | `string`              | Yes      | Agent that submitted the review.                                          |
| `status`     | `ApprovalStatus`      | Yes      | Current status of this review.                                            |
| `timestamp`  | `number`              | Yes      | Timestamp of the review action (ms since epoch).                          |
| `reason`     | `string \| undefined` | No       | Reason or comment provided with the review.                               |
| `atVersion`  | `number`              | Yes      | Version number the review applies to (`STALE` if document changed since). |

## ApprovalPolicy

Policy governing how approvals are evaluated.

| Property             | Type                  | Required | Description                                                                                                                                            |
| -------------------- | --------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `requiredCount`      | `number`              | Yes      | Minimum number of approvals required (absolute count).  Ignored when `requiredPercentage` is set ( 0).                                                 |
| `requireUnanimous`   | `boolean`             | Yes      | If true, all allowed reviewers must approve (overrides count/percentage).                                                                              |
| `allowedReviewerIds` | `string[]`            | Yes      | Agent IDs allowed to review. Empty means anyone can review.                                                                                            |
| `timeoutMs`          | `number`              | Yes      | Auto-expire reviews older than this (ms). 0 means no timeout.                                                                                          |
| `requiredPercentage` | `number \| undefined` | No       | Percentage of effective reviewers required (0-100). 0 means use requiredCount.  When  0, threshold = ceil(percentage \* effectiveReviewerCount / 100). |

## ApprovalResult

Result of evaluating reviews against a policy.

| Property      | Type       | Required | Description                                                 |
| ------------- | ---------- | -------- | ----------------------------------------------------------- |
| `approved`    | `boolean`  | Yes      | Whether the approval threshold is met.                      |
| `approvedBy`  | `string[]` | Yes      | Reviewers that have approved.                               |
| `rejectedBy`  | `string[]` | Yes      | Reviewers that have rejected.                               |
| `pendingFrom` | `string[]` | Yes      | Reviewers that are still pending.                           |
| `staleFrom`   | `string[]` | Yes      | Reviewers whose reviews are stale (document changed since). |
| `reason`      | `string`   | Yes      | Human-readable summary of the evaluation.                   |

## StorageType

How document content is stored.

```typescript
StorageType
```

## CompressionMethod

Compression method used for stored content.

```typescript
CompressionMethod
```

## ContentRef

Reference to where a document's compressed content lives.  Inline: content is stored directly in the database (small documents). Object-store: content is stored in S3-compatible storage, referenced by key.

| Property         | Type                  | Required | Description                                                                                                                                                     |
| ---------------- | --------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `type`           | `StorageType`         | Yes      | Storage backend type.                                                                                                                                           |
| `storageKey`     | `string \| undefined` | No       | Location of the content. - For `inline`: not used (content is in the database row). - For `object-store`: the object key (e.g. `attachments/xK9mP2nQ/v3.zlib`). |
| `contentHash`    | `string`              | Yes      | SHA-256 hash of the uncompressed content for integrity verification.                                                                                            |
| `originalSize`   | `number`              | Yes      | Size of the uncompressed content in bytes.                                                                                                                      |
| `compressedSize` | `number`              | Yes      | Size of the compressed content in bytes.                                                                                                                        |
| `compression`    | `CompressionMethod`   | Yes      | Compression method used.                                                                                                                                        |

## StorageMetadata

Metadata about a stored document blob.

| Property         | Type         | Required | Description                                          |
| ---------------- | ------------ | -------- | ---------------------------------------------------- |
| `ref`            | `ContentRef` | Yes      | Content reference.                                   |
| `createdAt`      | `number`     | Yes      | When the content was first stored (ms since epoch).  |
| `lastAccessedAt` | `number`     | Yes      | When the content was last accessed (ms since epoch). |
| `accessCount`    | `number`     | Yes      | Number of times the content has been accessed.       |

## StorageAdapter

Storage adapter that platforms implement.

| Property            | Type                                                                      | Required | Description                                                                                                     |
| ------------------- | ------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------- |
| `getContent`        | `(slug: string, version?: number) => Promise<string>`                     | No       | Retrieve document content at a specific version. When version is omitted, returns the latest version's content. |
| `putContent`        | `(slug: string, version: number, content: string) => Promise<ContentRef>` | Yes      | Store document content for a specific version. Returns a ContentRef indicating where the content was stored.    |
| `getVersions`       | `(slug: string) => Promise<VersionEntry[]>`                               | Yes      | Get the ordered list of version entries for a document.                                                         |
| `addVersion`        | `(slug: string, entry: VersionEntry) => Promise<void>`                    | Yes      | Append a new version entry to the document's version stack.                                                     |
| `getState`          | `(slug: string) => Promise<DocumentState>`                                | Yes      | Get the current lifecycle state of a document.                                                                  |
| `setState`          | `(slug: string, transition: StateTransition) => Promise<void>`            | Yes      | Record a lifecycle state transition.                                                                            |
| `getReviews`        | `(slug: string) => Promise<Review[]>`                                     | Yes      | Get all reviews for a document.                                                                                 |
| `addReview`         | `(slug: string, review: Review) => Promise<void>`                         | Yes      | Add or update a review for a document.                                                                          |
| `getApprovalPolicy` | `(slug: string) => Promise<ApprovalPolicy>`                               | Yes      | Get the approval policy for a document.                                                                         |

## VersionAttribution

Attribution data for a single version.

| Property           | Type       | Required | Description                                                           |
| ------------------ | ---------- | -------- | --------------------------------------------------------------------- |
| `versionNumber`    | `number`   | Yes      | Version number this attribution describes.                            |
| `authorId`         | `string`   | Yes      | Agent that authored the change.                                       |
| `addedLines`       | `number`   | Yes      | Lines added in this version.                                          |
| `removedLines`     | `number`   | Yes      | Lines removed in this version.                                        |
| `addedTokens`      | `number`   | Yes      | Tokens added in this version.                                         |
| `removedTokens`    | `number`   | Yes      | Tokens removed in this version.                                       |
| `sectionsModified` | `string[]` | Yes      | Section titles that were modified (detected via structural analysis). |
| `changelog`        | `string`   | Yes      | One-line change description.                                          |
| `createdAt`        | `number`   | Yes      | Timestamp of the change.                                              |

## ContributorSummary

Aggregated contribution summary for a single agent.

| Property             | Type       | Required | Description                                |
| -------------------- | ---------- | -------- | ------------------------------------------ |
| `agentId`            | `string`   | Yes      | Agent identifier.                          |
| `versionsAuthored`   | `number`   | Yes      | Number of versions this agent authored.    |
| `totalTokensAdded`   | `number`   | Yes      | Total tokens added across all versions.    |
| `totalTokensRemoved` | `number`   | Yes      | Total tokens removed across all versions.  |
| `netTokens`          | `number`   | Yes      | Net token impact (added - removed).        |
| `firstContribution`  | `number`   | Yes      | Timestamp of first contribution.           |
| `lastContribution`   | `number`   | Yes      | Timestamp of most recent contribution.     |
| `sectionsModified`   | `string[]` | Yes      | Unique section titles this agent modified. |

## PlannedSection

A section selected for retrieval.

| Property       | Type                                            | Required | Description                                         |
| -------------- | ----------------------------------------------- | -------- | --------------------------------------------------- |
| `sectionIndex` | `number`                                        | Yes      | Index into the overview's sections array.           |
| `title`        | `string`                                        | Yes      | Section title.                                      |
| `tokenCount`   | `number`                                        | Yes      | Estimated token cost for this section.              |
| `reason`       | `"query-match" \| "structural" \| "budget-fit"` | Yes      | Why this section was selected.                      |
| `score`        | `number \| undefined`                           | No       | Relevance score (0-1) when selected by query match. |

## RetrievalPlan

The output of retrieval planning.

| Property           | Type               | Required | Description                                                        |
| ------------------ | ------------------ | -------- | ------------------------------------------------------------------ |
| `sections`         | `PlannedSection[]` | Yes      | Sections selected for retrieval, ordered by relevance.             |
| `totalTokens`      | `number`           | Yes      | Total tokens across all selected sections.                         |
| `budgetRemaining`  | `number`           | Yes      | Remaining token budget after selection.                            |
| `tokensSaved`      | `number`           | Yes      | Tokens saved compared to fetching the full document.               |
| `fullDocumentFits` | `boolean`          | Yes      | Whether the full document fits within budget (no planning needed). |

## RetrievalOptions

Options for retrieval planning.

| Property       | Type                   | Required | Description                                                             |
| -------------- | ---------------------- | -------- | ----------------------------------------------------------------------- |
| `minScore`     | `number \| undefined`  | No       | Minimum similarity score to include a section (0-1). Default: 0.1       |
| `includeIntro` | `boolean \| undefined` | No       | Always include the first section (typically title/intro). Default: true |

## CreateVersionOptions

Options for creating a new version.

| Property    | Type     | Required | Description                  |
| ----------- | -------- | -------- | ---------------------------- |
| `agentId`   | `string` | Yes      | Agent creating this version. |
| `changelog` | `string` | Yes      | One-line change description. |

## LlmtxtDocumentOptions

Options for constructing an LlmtxtDocument.

| Property  | Type             | Required | Description                      |
| --------- | ---------------- | -------- | -------------------------------- |
| `slug`    | `string`         | Yes      | Document slug.                   |
| `storage` | `StorageAdapter` | Yes      | Storage adapter for persistence. |

## Lease

| Property    | Type     | Required | Description |
| ----------- | -------- | -------- | ----------- |
| `leaseId`   | `string` | Yes      | leaseId     |
| `holder`    | `string` | Yes      | holder      |
| `expiresAt` | `string` | Yes      | expiresAt   |

## LeaseOptions

| Property  | Type     | Required | Description |
| --------- | -------- | -------- | ----------- |
| `baseUrl` | `string` | Yes      | baseUrl     |
| `apiKey`  | `string` | Yes      | apiKey      |

## SectionDelta

Section-level delta returned by fetchSectionDelta and included in diff-mode SSE payloads.

| Property   | Type                                   | Required | Description |
| ---------- | -------------------------------------- | -------- | ----------- |
| `added`    | `string[]`                             | Yes      | added       |
| `modified` | `{ name: string; content: string; }[]` | Yes      | modified    |
| `deleted`  | `string[]`                             | Yes      | deleted     |
| `fromSeq`  | `number`                               | Yes      | fromSeq     |
| `toSeq`    | `number`                               | Yes      | toSeq       |

## SectionDeltaResponse

| Property     | Type                   | Required | Description |
| ------------ | ---------------------- | -------- | ----------- |
| `delta`      | `SectionDelta \| null` | Yes      | delta       |
| `currentSeq` | `number`               | Yes      | currentSeq  |

## SubscribeOptions

| Property  | Type                              | Required | Description                                                                      |
| --------- | --------------------------------- | -------- | -------------------------------------------------------------------------------- |
| `since`   | `number \| undefined`             | No       | Resume from a specific sequence number (sent as Last-Event-ID).                  |
| `mode`    | `"events" \| "diff" \| undefined` | No       | 'events' = raw event payloads; 'diff' = payloads include computed content diffs. |
| `baseUrl` | `string`                          | Yes      | Base URL of the API (no trailing slash).                                         |
| `apiKey`  | `string`                          | Yes      | Bearer API key.                                                                  |

## SubscriptionEvent

| Property  | Type                        | Required | Description                                                                |
| --------- | --------------------------- | -------- | -------------------------------------------------------------------------- |
| `seq`     | `number`                    | Yes      | Monotonic sequence number (from Last-Event-ID).                            |
| `type`    | `string`                    | Yes      | Event type string (e.g. 'version.published', 'SECTION\_LEASED').           |
| `path`    | `string`                    | Yes      | The path pattern the subscription was opened with.                         |
| `payload` | `unknown`                   | Yes      | Raw event payload.                                                         |
| `delta`   | `SectionDelta \| undefined` | No       | Section-level delta, present when mode='diff' and the server computed one. |

## Unsubscribe

Call to close the SSE subscription.

```typescript
Unsubscribe
```

## TopologyMode

The three supported deployment topologies.  - `standalone` — one agent, one local .db file, zero network dependency. - `hub-spoke` — N ephemeral or persistent spokes connected to a central hub. - `mesh` — N persistent peers syncing via P2P transport (T386).

```typescript
TopologyMode
```

## StandaloneConfig

Config for standalone topology.  One agent, one local `.db` file, zero network dependency. Use for single developer or single agent, offline-first operation, local testing.

| Property          | Type                   | Required | Description                                                 |
| ----------------- | ---------------------- | -------- | ----------------------------------------------------------- |
| `topology`        | `"standalone"`         | Yes      | topology                                                    |
| `storagePath`     | `string \| undefined`  | No       | Path for the local .db file. Defaults to '.llmtxt'.         |
| `identityPath`    | `string \| undefined`  | No       | Optional path to agent identity keypair.                    |
| `crsqlite`        | `boolean \| undefined` | No       | Set true to enable cr-sqlite (T385). Default: false.        |
| `crsqliteExtPath` | `string \| undefined`  | No       | Path to crsqlite extension (optional, see P2-cr-sqlite.md). |

## HubSpokeConfig

Config for hub-and-spoke topology.  One hub (PostgresBackend or a designated LocalBackend) is the Single Source of Truth. N spokes are RemoteBackend clients that write to and read from the hub. Ephemeral swarm workers are spokes with no local `.db` file.

| Property         | Type                   | Required | Description                                                                                                                                                       |
| ---------------- | ---------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `topology`       | `"hub-spoke"`          | Yes      | topology                                                                                                                                                          |
| `hubUrl`         | `string`               | Yes      | URL of the hub API instance (e.g. '<https://api.llmtxt.my>'). REQUIRED — validation MUST fail fast if absent.                                                     |
| `apiKey`         | `string \| undefined`  | No       | API key for authenticating with the hub. MUST be present for write operations.                                                                                    |
| `identityPath`   | `string \| undefined`  | No       | Ed25519 private key hex for signing writes (alternative to apiKey). If both are supplied, Ed25519 signed writes take precedence.                                  |
| `persistLocally` | `boolean \| undefined` | No       | When true, this spoke maintains a local cr-sqlite replica. Requires T385 (cr-sqlite) to be installed. Default: false (ephemeral swarm worker mode — no .db file). |
| `storagePath`    | `string \| undefined`  | No       | Required when persistLocally=true. Path to local .db file.                                                                                                        |

## MeshConfig

Config for mesh topology.  N persistent peers, each with their own cr-sqlite LocalBackend. No central hub is required. Peers sync directly with each other via the P2P transport defined in T386. Use for offline-first P2P collaboration, air-gapped environments, or small teams of persistent agents (≤10 peers).

| Property       | Type                            | Required | Description                                                                                                          |
| -------------- | ------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------- |
| `topology`     | `"mesh"`                        | Yes      | topology                                                                                                             |
| `storagePath`  | `string`                        | Yes      | Path for the local cr-sqlite .db file. REQUIRED for mesh.                                                            |
| `identityPath` | `string \| undefined`           | No       | Optional path to agent identity keypair. Defaults to storagePath/identity.json.                                      |
| `peers`        | `string[] \| undefined`         | No       | Known peers at startup. Each entry is a transport address. Format: 'unix:/path/to/sock' \| '<http://host:port>'      |
| `meshDir`      | `string \| undefined`           | No       | Directory where peer advertisement files are written and read. Defaults to $LLMTXT\_MESH\_DIR or '/tmp/llmtxt-mesh'. |
| `transport`    | `"unix" \| "http" \| undefined` | No       | Transport to listen on. Default: 'unix'.                                                                             |
| `port`         | `number \| undefined`           | No       | Port for http transport. Default: 7642.                                                                              |

## TopologyConfig

Discriminated union of all topology configs, keyed on the `topology` field.

```typescript
TopologyConfig
```

## BackendConfig

Configuration for a Backend instance.  BackendConfig is passed to the backend constructor. LocalBackend uses storagePath + identityPath; RemoteBackend uses baseUrl + apiKey.

| Property                | Type                           | Required | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------- | ------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `storagePath`           | `string \| undefined`          | No       | Directory where the backend stores its data. For LocalBackend: SQLite DB and large content blobs live here. Defaults to '.llmtxt' relative to the working directory.                                                                                                                                                                                                                                                                                                                                      |
| `identityPath`          | `string \| undefined`          | No       | Path to the agent identity keypair JSON file. Defaults to /identity.json.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `baseUrl`               | `string \| undefined`          | No       | Base URL of a remote LLMtxt API instance. Required for RemoteBackend. MUST include scheme (https\://).                                                                                                                                                                                                                                                                                                                                                                                                    |
| `apiKey`                | `string \| undefined`          | No       | API key for authenticating with the remote instance. Used by RemoteBackend in the Authorization header.                                                                                                                                                                                                                                                                                                                                                                                                   |
| `wal`                   | `boolean \| undefined`         | No       | SQLite WAL mode. Defaults to true. Only relevant for LocalBackend.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `leaseReaperIntervalMs` | `number \| undefined`          | No       | Lease reaper interval in milliseconds. Defaults to 10\_000. Only relevant for LocalBackend.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `presenceTtlMs`         | `number \| undefined`          | No       | Presence TTL in milliseconds. Defaults to 30\_000. Only relevant for LocalBackend.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `crsqliteExtPath`       | `string \| undefined`          | No       | Absolute path to a pre-downloaded crsqlite native extension (.so / .dylib / .dll). When supplied, LocalBackend uses this path instead of resolving the extension via vlcn.io/crsqlite. Useful in air-gapped or bundled environments where the install-time binary download is not possible.  DR-P2-01: If absent, LocalBackend attempts to resolve the path from the vlcn.io/crsqlite optional peer dependency. If neither is available, LocalBackend opens without cr-sqlite (hasCRR = false, no crash). |
| `maxBlobSizeBytes`      | `number \| undefined`          | No       | Maximum blob size in bytes. Defaults to 100 \* 1024 \* 1024 (100 MB).                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `blobStorageMode`       | `"s3" \| "pg-lo" \| undefined` | No       | Blob storage mode for PostgresBackend. 's3' uses S3/R2 object storage (default). 'pg-lo' uses PostgreSQL large objects.                                                                                                                                                                                                                                                                                                                                                                                   |
| `s3Endpoint`            | `string \| undefined`          | No       | S3/R2 endpoint URL (e.g. "<https://s3.us-east-1.amazonaws.com>").                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `s3Bucket`              | `string \| undefined`          | No       | S3/R2 bucket name. Required when blobStorageMode = 's3'.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `s3Region`              | `string \| undefined`          | No       | S3/R2 region (e.g. "us-east-1").                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `s3AccessKeyId`         | `string \| undefined`          | No       | S3/R2 access key ID.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `s3SecretAccessKey`     | `string \| undefined`          | No       | S3/R2 secret access key.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## Document

A stored document record.

| Property       | Type                    | Required | Description                                               |
| -------------- | ----------------------- | -------- | --------------------------------------------------------- |
| `id`           | `string`                | Yes      | Unique document identifier (nanoid).                      |
| `slug`         | `string`                | Yes      | URL-safe slug derived from the title. Unique per backend. |
| `title`        | `string`                | Yes      | Human-readable document title.                            |
| `state`        | `DocumentState`         | Yes      | Current lifecycle state.                                  |
| `createdBy`    | `string`                | Yes      | Agent that created the document.                          |
| `createdAt`    | `number`                | Yes      | Creation timestamp (ms since epoch).                      |
| `updatedAt`    | `number`                | Yes      | Last modified timestamp (ms since epoch).                 |
| `versionCount` | `number`                | Yes      | Current version count.                                    |
| `labels`       | `string[] \| undefined` | No       | Arbitrary metadata labels.                                |

## CreateDocumentParams

Parameters for creating a document.

| Property    | Type                    | Required | Description                                                          |
| ----------- | ----------------------- | -------- | -------------------------------------------------------------------- |
| `title`     | `string`                | Yes      | title                                                                |
| `createdBy` | `string`                | Yes      | createdBy                                                            |
| `labels`    | `string[] \| undefined` | No       | labels                                                               |
| `slug`      | `string \| undefined`   | No       | If supplied, slug is used as-is instead of being derived from title. |

## ListDocumentsParams

Parameters for listing documents.

| Property    | Type                         | Required | Description                                |
| ----------- | ---------------------------- | -------- | ------------------------------------------ |
| `cursor`    | `string \| undefined`        | No       | Cursor for pagination (document id).       |
| `limit`     | `number \| undefined`        | No       | Maximum number of results. Defaults to 20. |
| `state`     | `DocumentState \| undefined` | No       | Filter by state.                           |
| `createdBy` | `string \| undefined`        | No       | Filter by creator.                         |

## ListResult

Paginated list result.

| Property     | Type             | Required | Description                                                   |
| ------------ | ---------------- | -------- | ------------------------------------------------------------- |
| `items`      | `T[]`            | Yes      | items                                                         |
| `nextCursor` | `string \| null` | Yes      | Cursor to pass for the next page, or null if no more results. |

## PublishVersionParams

Parameters for publishing a new version.

| Property     | Type     | Required | Description                                                         |
| ------------ | -------- | -------- | ------------------------------------------------------------------- |
| `documentId` | `string` | Yes      | documentId                                                          |
| `content`    | `string` | Yes      | Full content of this version (before patching).                     |
| `patchText`  | `string` | Yes      | Unified diff patch text from previous version. Empty string for v1. |
| `createdBy`  | `string` | Yes      | Agent creating this version.                                        |
| `changelog`  | `string` | Yes      | One-line description of the change.                                 |

## TransitionParams

Parameters for transitioning a document's lifecycle state.

| Property     | Type                  | Required | Description |
| ------------ | --------------------- | -------- | ----------- |
| `documentId` | `string`              | Yes      | documentId  |
| `to`         | `DocumentState`       | Yes      | to          |
| `changedBy`  | `string`              | Yes      | changedBy   |
| `reason`     | `string \| undefined` | No       | reason      |

## DocumentEvent

A single document event entry.

| Property     | Type                      | Required | Description |
| ------------ | ------------------------- | -------- | ----------- |
| `id`         | `string`                  | Yes      | id          |
| `documentId` | `string`                  | Yes      | documentId  |
| `type`       | `string`                  | Yes      | type        |
| `agentId`    | `string`                  | Yes      | agentId     |
| `payload`    | `Record<string, unknown>` | Yes      | payload     |
| `createdAt`  | `number`                  | Yes      | createdAt   |

## AppendEventParams

Parameters for appending an event.

| Property     | Type                                   | Required | Description |
| ------------ | -------------------------------------- | -------- | ----------- |
| `documentId` | `string`                               | Yes      | documentId  |
| `type`       | `string`                               | Yes      | type        |
| `agentId`    | `string`                               | Yes      | agentId     |
| `payload`    | `Record<string, unknown> \| undefined` | No       | payload     |

## QueryEventsParams

Parameters for querying events.

| Property     | Type                  | Required | Description                                 |
| ------------ | --------------------- | -------- | ------------------------------------------- |
| `documentId` | `string`              | Yes      | documentId                                  |
| `type`       | `string \| undefined` | No       | Filter by event type.                       |
| `since`      | `string \| undefined` | No       | Return events after this cursor (event id). |
| `limit`      | `number \| undefined` | No       | Maximum results. Defaults to 50.            |

## CrdtUpdate

A CRDT update payload for a document section.

| Property       | Type     | Required | Description                          |
| -------------- | -------- | -------- | ------------------------------------ |
| `documentId`   | `string` | Yes      | documentId                           |
| `sectionKey`   | `string` | Yes      | sectionKey                           |
| `updateBase64` | `string` | Yes      | Yjs binary update encoded as base64. |
| `agentId`      | `string` | Yes      | agentId                              |
| `createdAt`    | `number` | Yes      | createdAt                            |

## CrdtState

Current CRDT state for a section.

| Property            | Type     | Required | Description                              |
| ------------------- | -------- | -------- | ---------------------------------------- |
| `documentId`        | `string` | Yes      | documentId                               |
| `sectionKey`        | `string` | Yes      | sectionKey                               |
| `stateVectorBase64` | `string` | Yes      | Serialized Yjs state vector as base64.   |
| `snapshotBase64`    | `string` | Yes      | Current merged document state as base64. |
| `updatedAt`         | `number` | Yes      | updatedAt                                |

## Lease

A distributed lock / lease record.

| Property     | Type     | Required | Description                                           |
| ------------ | -------- | -------- | ----------------------------------------------------- |
| `id`         | `string` | Yes      | id                                                    |
| `resource`   | `string` | Yes      | Resource being locked (e.g. 'document:abc123').       |
| `holder`     | `string` | Yes      | Agent holding the lease.                              |
| `expiresAt`  | `number` | Yes      | Expiry timestamp (ms since epoch). 0 = never expires. |
| `acquiredAt` | `number` | Yes      | acquiredAt                                            |

## AcquireLeaseParams

Parameters for acquiring a lease.

| Property   | Type     | Required | Description                |
| ---------- | -------- | -------- | -------------------------- |
| `resource` | `string` | Yes      | resource                   |
| `holder`   | `string` | Yes      | holder                     |
| `ttlMs`    | `number` | Yes      | Lease TTL in milliseconds. |

## PresenceEntry

A presence record for an agent viewing a document.

| Property     | Type                                   | Required | Description                                    |
| ------------ | -------------------------------------- | -------- | ---------------------------------------------- |
| `agentId`    | `string`                               | Yes      | agentId                                        |
| `documentId` | `string`                               | Yes      | documentId                                     |
| `meta`       | `Record<string, unknown> \| undefined` | No       | Agent metadata (cursor position, color, etc.). |
| `lastSeen`   | `number`                               | Yes      | lastSeen                                       |
| `expiresAt`  | `number`                               | Yes      | Expiry timestamp (ms since epoch).             |

## ScratchpadMessage

A scratchpad message entry.

| Property      | Type                      | Required | Description                                 |
| ------------- | ------------------------- | -------- | ------------------------------------------- |
| `id`          | `string`                  | Yes      | id                                          |
| `toAgentId`   | `string`                  | Yes      | Recipient agent id.                         |
| `fromAgentId` | `string`                  | Yes      | Sender agent id.                            |
| `payload`     | `Record<string, unknown>` | Yes      | payload                                     |
| `createdAt`   | `number`                  | Yes      | createdAt                                   |
| `exp`         | `number`                  | Yes      | Expiry (ms since epoch). 0 = never expires. |

## SendScratchpadParams

Parameters for sending a scratchpad message.

| Property      | Type                      | Required | Description                                    |
| ------------- | ------------------------- | -------- | ---------------------------------------------- |
| `toAgentId`   | `string`                  | Yes      | toAgentId                                      |
| `fromAgentId` | `string`                  | Yes      | fromAgentId                                    |
| `payload`     | `Record<string, unknown>` | Yes      | payload                                        |
| `ttlMs`       | `number \| undefined`     | No       | TTL in ms. 0 = never expires. Defaults to 24h. |

## A2AMessage

An A2A (Agent-to-Agent) inbox message.

| Property       | Type     | Required | Description                                 |
| -------------- | -------- | -------- | ------------------------------------------- |
| `id`           | `string` | Yes      | id                                          |
| `toAgentId`    | `string` | Yes      | Recipient agent id.                         |
| `envelopeJson` | `string` | Yes      | Ed25519-signed envelope JSON string.        |
| `createdAt`    | `number` | Yes      | createdAt                                   |
| `exp`          | `number` | Yes      | Expiry (ms since epoch). 0 = never expires. |

## SearchResult

A single semantic search result.

| Property     | Type                  | Required | Description                         |
| ------------ | --------------------- | -------- | ----------------------------------- |
| `documentId` | `string`              | Yes      | documentId                          |
| `slug`       | `string`              | Yes      | slug                                |
| `title`      | `string`              | Yes      | title                               |
| `score`      | `number`              | Yes      | Cosine similarity score in \[0, 1]. |
| `snippet`    | `string \| undefined` | No       | Matching snippet (optional).        |

## SearchParams

Parameters for semantic search.

| Property   | Type                  | Required | Description                                      |
| ---------- | --------------------- | -------- | ------------------------------------------------ |
| `query`    | `string`              | Yes      | query                                            |
| `topK`     | `number \| undefined` | No       | Maximum results. Defaults to 10.                 |
| `minScore` | `number \| undefined` | No       | Minimum similarity score (0–1). Defaults to 0.0. |

## AgentPubkeyRecord

A registered agent public key record.

| Property    | Type                  | Required | Description                        |
| ----------- | --------------------- | -------- | ---------------------------------- |
| `agentId`   | `string`              | Yes      | agentId                            |
| `pubkeyHex` | `string`              | Yes      | Hex-encoded Ed25519 public key.    |
| `label`     | `string \| undefined` | No       | label                              |
| `createdAt` | `number`              | Yes      | createdAt                          |
| `revokedAt` | `number \| undefined` | No       | If set, this key has been revoked. |

## AttachBlobParams

Parameters for attaching a blob to a document.

| Property      | Type                                                     | Required | Description                                        |
| ------------- | -------------------------------------------------------- | -------- | -------------------------------------------------- |
| `docSlug`     | `string`                                                 | Yes      | Document slug the blob is attached to.             |
| `name`        | `string`                                                 | Yes      | User-visible attachment name (e.g. "diagram.png"). |
| `contentType` | `string`                                                 | Yes      | MIME content type.                                 |
| `data`        | `Uint8Array<ArrayBufferLike> \| Buffer<ArrayBufferLike>` | Yes      | Raw binary data.                                   |
| `uploadedBy`  | `string`                                                 | Yes      | Agent performing the upload.                       |

## BlobAttachment

A stored blob attachment record.

| Property      | Type     | Required | Description |
| ------------- | -------- | -------- | ----------- |
| `id`          | `string` | Yes      | id          |
| `docSlug`     | `string` | Yes      | docSlug     |
| `blobName`    | `string` | Yes      | blobName    |
| `hash`        | `string` | Yes      | hash        |
| `size`        | `number` | Yes      | size        |
| `contentType` | `string` | Yes      | contentType |
| `uploadedBy`  | `string` | Yes      | uploadedBy  |
| `uploadedAt`  | `number` | Yes      | uploadedAt  |

## BlobData

Result of fetching a blob.

| Property | Type                                   | Required | Description                                                      |
| -------- | -------------------------------------- | -------- | ---------------------------------------------------------------- |
| `data`   | `Buffer<ArrayBufferLike> \| undefined` | No       | Raw blob bytes. Only present when fetched with includeData=true. |

## BlobRef

Changeset blob reference (bytes omitted — lazy pull).

| Property      | Type     | Required | Description |
| ------------- | -------- | -------- | ----------- |
| `blobName`    | `string` | Yes      | blobName    |
| `hash`        | `string` | Yes      | hash        |
| `size`        | `number` | Yes      | size        |
| `contentType` | `string` | Yes      | contentType |
| `uploadedBy`  | `string` | Yes      | uploadedBy  |
| `uploadedAt`  | `number` | Yes      | uploadedAt  |

## BlobOps

Blob storage and retrieval operations.

| Property          | Type                                                                                                  | Required | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------- | ----------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `attachBlob`      | `(params: AttachBlobParams) => Promise<BlobAttachment>`                                               | Yes      | Attach a binary blob to a document.  MUST compute SHA-256 hash of data and use it as the storage key. MUST validate the attachment name via llmtxt-core blob\_name\_validate. MUST enforce maxBlobSizeBytes (default 100MB). MUST apply LWW: if a blob with the same name already exists on the document,   it is soft-deleted and the new record becomes active. MUST NOT store duplicate bytes when hash already exists in the store   (content-addressed dedup within the same backend instance). MUST return the new BlobAttachment record. |
| `getBlob`         | `(docSlug: string, blobName: string, opts?: { includeData?: boolean; }) => Promise<BlobData \| null>` | No       | Retrieve a blob attachment, optionally including bytes.  MUST return null (not throw) if blobName is not attached to the document. MUST verify hash on read when includeData=true. Return BlobCorruptError if mismatch. Default: includeData = false (manifest metadata only).                                                                                                                                                                                                                                                                  |
| `listBlobs`       | `(docSlug: string) => Promise<BlobAttachment[]>`                                                      | Yes      | List all active (non-deleted) blob attachments for a document.  MUST return an empty array (not throw) when no blobs are attached. MUST NOT include bytes (manifest metadata only).                                                                                                                                                                                                                                                                                                                                                             |
| `detachBlob`      | `(docSlug: string, blobName: string, detachedBy: string) => Promise<boolean>`                         | Yes      | Detach (soft-delete) a named blob from a document.  MUST return false (not throw) if no active attachment with blobName exists. MUST set deleted\_at = now(). Actual byte storage is NOT cleaned up   (orphan collection is a deferred concern). MUST NOT affect other documents sharing the same blob hash.                                                                                                                                                                                                                                    |
| `fetchBlobByHash` | `(hash: string) => Promise<Buffer \| null>`                                                           | Yes      | Fetch blob bytes by hash directly (used during lazy sync pull).  MUST return null if no blob with this hash exists in the store. MUST verify hash on return. This method bypasses the manifest and is used by the sync layer.                                                                                                                                                                                                                                                                                                                   |

## DocumentOps

Document CRUD operations.

| Property            | Type                                                              | Required | Description                                                                                              |
| ------------------- | ----------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------- |
| `createDocument`    | `(params: CreateDocumentParams) => Promise<Document>`             | Yes      | Create a new document. MUST generate a unique slug from the title if not provided.                       |
| `getDocument`       | `(id: string) => Promise<Document \| null>`                       | Yes      | Retrieve a document by its id. MUST return null (not throw) when the document does not exist.            |
| `getDocumentBySlug` | `(slug: string) => Promise<Document \| null>`                     | Yes      | Retrieve a document by its slug. MUST return null (not throw) when the document does not exist.          |
| `listDocuments`     | `(params?: ListDocumentsParams) => Promise<ListResult<Document>>` | No       | List documents with optional filtering and cursor-based pagination.                                      |
| `deleteDocument`    | `(id: string) => Promise<boolean>`                                | Yes      | Delete a document and all associated data. MUST return false (not throw) if the document does not exist. |

## VersionOps

Version stack operations.

| Property            | Type                                                                                                | Required | Description                                                                                                                                                                     |
| ------------------- | --------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `publishVersion`    | `(params: PublishVersionParams) => Promise<VersionEntry>`                                           | Yes      | Publish a new version of a document. MUST compute and store the content hash via llmtxt-core hash\_content. MUST increment the document's versionCount.                         |
| `getVersion`        | `(documentId: string, versionNumber: number) => Promise<VersionEntry \| null>`                      | Yes      | Retrieve a version entry by document id and version number. MUST return null when the version does not exist.                                                                   |
| `listVersions`      | `(documentId: string) => Promise<VersionEntry[]>`                                                   | Yes      | List all version entries for a document in ascending order.                                                                                                                     |
| `transitionVersion` | `(params: TransitionParams) => Promise<{ success: boolean; error?: string; document?: Document; }>` | No       | Transition a document's lifecycle state. MUST validate the transition via sdk/lifecycle.ts validateTransition. MUST return an error result (not throw) for invalid transitions. |

## ApprovalOps

BFT approval operations.

| Property               | Type                                                                                                                                                                                                                                  | Required | Description                                                                                                                                                                  |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `submitSignedApproval` | `(params: { documentId: string; versionNumber: number; reviewerId: string; status: "APPROVED" \| "REJECTED"; reason?: string; signatureBase64: string; }) => Promise<{ success: boolean; error?: string; result?: ApprovalResult; }>` | No       | Submit a signed approval (or rejection) for a document version. MUST verify the Ed25519 signature before persisting. MUST reject duplicate approvals from the same reviewer. |
| `getApprovalProgress`  | `(documentId: string, versionNumber: number) => Promise<ApprovalResult>`                                                                                                                                                              | Yes      | Get current approval progress for a document version.                                                                                                                        |
| `getApprovalPolicy`    | `(documentId: string) => Promise<ApprovalPolicy>`                                                                                                                                                                                     | Yes      | Get or set the approval policy for a document.                                                                                                                               |
| `setApprovalPolicy`    | `(documentId: string, policy: ApprovalPolicy) => Promise<void>`                                                                                                                                                                       | Yes      | setApprovalPolicy                                                                                                                                                            |

## EventOps

Document event log operations.

| Property          | Type                                                                | Required | Description                                                                                                                                                                                                                                                               |
| ----------------- | ------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `appendEvent`     | `(params: AppendEventParams) => Promise<DocumentEvent>`             | Yes      | Append an event to a document's event log. MUST emit the event on the local bus for subscribeStream consumers.                                                                                                                                                            |
| `queryEvents`     | `(params: QueryEventsParams) => Promise<ListResult<DocumentEvent>>` | Yes      | Query the event log for a document with optional filtering.                                                                                                                                                                                                               |
| `subscribeStream` | `(documentId: string) => AsyncIterable<DocumentEvent>`              | Yes      | Subscribe to the event stream for a document. MUST return an AsyncIterable that yields events as they are appended. MUST clean up listeners when the consumer calls .return() or the iterator is GC'd. LocalBackend uses in-process EventEmitter; RemoteBackend uses SSE. |

## CrdtOps

CRDT section operations.

| Property           | Type                                                                                                                 | Required | Description                                                                                                                                                   |
| ------------------ | -------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `applyCrdtUpdate`  | `(params: { documentId: string; sectionKey: string; updateBase64: string; agentId: string; }) => Promise<CrdtState>` | Yes      | Apply a Yjs binary update to a document section. MUST merge via llmtxt-core WASM merge\_updates. MUST persist the raw update and update the section snapshot. |
| `getCrdtState`     | `(documentId: string, sectionKey: string) => Promise<CrdtState \| null>`                                             | Yes      | Get the current CRDT state for a section. MUST return null when no state exists for the section.                                                              |
| `subscribeSection` | `(documentId: string, sectionKey: string) => AsyncIterable<CrdtUpdate>`                                              | Yes      | Subscribe to CRDT updates for a document section. LocalBackend uses in-process EventEmitter; RemoteBackend uses WS.                                           |

## LeaseOps

Distributed lease operations.

| Property       | Type                                                                          | Required | Description                                                                                                                                                                 |
| -------------- | ----------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `acquireLease` | `(params: AcquireLeaseParams) => Promise<Lease \| null>`                      | Yes      | Acquire a lease on a resource. MUST fail (return null) if a non-expired lease exists for a different holder. MUST succeed (return existing) if the same holder re-acquires. |
| `renewLease`   | `(resource: string, holder: string, ttlMs: number) => Promise<Lease \| null>` | Yes      | Renew an existing lease, extending its TTL. MUST return null if the lease does not exist or is held by a different agent.                                                   |
| `releaseLease` | `(resource: string, holder: string) => Promise<boolean>`                      | Yes      | Release a lease immediately. MUST return false if the lease does not exist or holder mismatch.                                                                              |
| `getLease`     | `(resource: string) => Promise<Lease \| null>`                                | Yes      | Get the current lease for a resource. Returns null if no active lease exists.                                                                                               |

## PresenceOps

Presence (real-time who-is-viewing) operations.

| Property            | Type                                                                                              | Required | Description                                                                                                     |
| ------------------- | ------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------- |
| `joinPresence`      | `(documentId: string, agentId: string, meta?: Record<string, unknown>) => Promise<PresenceEntry>` | No       | Join or update presence for an agent on a document. Presence is NOT persisted across restarts — in-memory only. |
| `leavePresence`     | `(documentId: string, agentId: string) => Promise<void>`                                          | Yes      | Remove an agent from a document's presence.                                                                     |
| `listPresence`      | `(documentId: string) => Promise<PresenceEntry[]>`                                                | Yes      | List all non-expired presence entries for a document.                                                           |
| `heartbeatPresence` | `(documentId: string, agentId: string) => Promise<void>`                                          | Yes      | Refresh the lastSeen timestamp for an agent's presence.                                                         |

## ScratchpadOps

Scratchpad ephemeral message operations.

| Property                  | Type                                                                | Required | Description                                                                                                              |
| ------------------------- | ------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------ |
| `sendScratchpad`          | `(params: SendScratchpadParams) => Promise<ScratchpadMessage>`      | Yes      | Send a scratchpad message to an agent. Default TTL is 24 hours. exp=0 means never expires.                               |
| `pollScratchpad`          | `(agentId: string, limit?: number) => Promise<ScratchpadMessage[]>` | No       | Poll scratchpad messages for an agent. MUST only return non-expired messages. MUST treat exp=0 entries as never-expired. |
| `deleteScratchpadMessage` | `(id: string, agentId: string) => Promise<boolean>`                 | Yes      | Delete scratchpad messages for an agent (after consumption).                                                             |

## A2AOps

A2A (Agent-to-Agent) inbox operations.

| Property           | Type                                                                                                                                             | Required | Description                                                                                                                              |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `sendA2AMessage`   | `(params: { toAgentId: string; envelopeJson: string; ttlMs?: number; }) => Promise<{ success: boolean; error?: string; message?: A2AMessage; }>` | No       | Deliver a signed A2A message to an agent's inbox. MUST verify the sender's Ed25519 signature before persisting. Default TTL is 48 hours. |
| `pollA2AInbox`     | `(agentId: string, limit?: number, since?: number, order?: "asc" \| "desc") => Promise<A2AMessage[]>`                                            | No       | Poll messages from an agent's inbox. MUST only return non-expired messages.                                                              |
| `deleteA2AMessage` | `(id: string, agentId: string) => Promise<boolean>`                                                                                              | Yes      | Delete a message from an agent's inbox.                                                                                                  |

## SearchOps

Semantic search operations.

| Property        | Type                                                     | Required | Description                                                                                                                                                                              |
| --------------- | -------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `indexDocument` | `(documentId: string, content: string) => Promise<void>` | Yes      | Index a document version for semantic search. MUST compute an embedding vector and store it. SHOULD degrade gracefully when onnxruntime-node is not installed.                           |
| `search`        | `(params: SearchParams) => Promise<SearchResult[]>`      | Yes      | Perform semantic search across indexed documents. MUST return results sorted by cosine similarity descending. SHOULD return empty array (not throw) when embedding model is unavailable. |

## IdentityOps

Agent identity and pubkey registry operations.

| Property               | Type                                                                                 | Required | Description                                                                                                                |
| ---------------------- | ------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------- |
| `registerAgentPubkey`  | `(agentId: string, pubkeyHex: string, label?: string) => Promise<AgentPubkeyRecord>` | No       | Register an agent's public key. MUST be idempotent — registering the same key twice MUST NOT error.                        |
| `lookupAgentPubkey`    | `(agentId: string) => Promise<AgentPubkeyRecord \| null>`                            | Yes      | Look up an agent's registered public key. MUST return null when the agent has no registered key.                           |
| `listAgentPubkeys`     | `(userId?: string) => Promise<AgentPubkeyRecord[]>`                                  | No       | List all active (non-revoked) public key records. SHOULD support optional userId filter when the backend tracks ownership. |
| `revokeAgentPubkey`    | `(agentId: string, pubkeyHex: string) => Promise<boolean>`                           | Yes      | Revoke an agent's public key. MUST set revokedAt on the key record.                                                        |
| `recordSignatureNonce` | `(agentId: string, nonce: string, ttlMs?: number) => Promise<boolean>`               | No       | Record a signature nonce to prevent replay attacks. MUST fail if the nonce has already been recorded.                      |
| `hasNonceBeenUsed`     | `(agentId: string, nonce: string) => Promise<boolean>`                               | Yes      | Check whether a nonce has already been used.                                                                               |

## ContributorRecord

A contributor record tracking per-agent token attribution.

| Property            | Type     | Required | Description                                                    |
| ------------------- | -------- | -------- | -------------------------------------------------------------- |
| `documentId`        | `string` | Yes      | documentId                                                     |
| `agentId`           | `string` | Yes      | agentId                                                        |
| `netTokens`         | `number` | Yes      | Net token contribution (positive = added, negative = removed). |
| `versionCount`      | `number` | Yes      | Number of versions this agent has contributed.                 |
| `lastContributedAt` | `number` | Yes      | lastContributedAt                                              |

## Collection

A named collection of documents.

| Property        | Type                  | Required | Description                                        |
| --------------- | --------------------- | -------- | -------------------------------------------------- |
| `id`            | `string`              | Yes      | id                                                 |
| `slug`          | `string`              | Yes      | slug                                               |
| `name`          | `string`              | Yes      | name                                               |
| `description`   | `string \| undefined` | No       | description                                        |
| `ownerId`       | `string`              | Yes      | ownerId                                            |
| `createdAt`     | `number`              | Yes      | createdAt                                          |
| `updatedAt`     | `number`              | Yes      | updatedAt                                          |
| `documentSlugs` | `string[]`            | Yes      | Ordered list of document slugs in this collection. |

## CreateCollectionParams

Parameters for creating a collection.

| Property      | Type                  | Required | Description |
| ------------- | --------------------- | -------- | ----------- |
| `name`        | `string`              | Yes      | name        |
| `description` | `string \| undefined` | No       | description |
| `ownerId`     | `string`              | Yes      | ownerId     |
| `slug`        | `string \| undefined` | No       | slug        |

## ListCollectionsParams

Parameters for listing collections.

| Property  | Type                  | Required | Description |
| --------- | --------------------- | -------- | ----------- |
| `ownerId` | `string \| undefined` | No       | ownerId     |
| `cursor`  | `string \| undefined` | No       | cursor      |
| `limit`   | `number \| undefined` | No       | limit       |

## CollectionExport

Export format for a collection.

| Property     | Type         | Required | Description |
| ------------ | ------------ | -------- | ----------- |
| `collection` | `Collection` | Yes      | collection  |
| `documents`  | `Document[]` | Yes      | documents   |
| `exportedAt` | `number`     | Yes      | exportedAt  |

## DocumentLink

A directed link between two documents.

| Property           | Type                  | Required | Description                                          |
| ------------------ | --------------------- | -------- | ---------------------------------------------------- |
| `id`               | `string`              | Yes      | id                                                   |
| `sourceDocumentId` | `string`              | Yes      | sourceDocumentId                                     |
| `targetDocumentId` | `string`              | Yes      | targetDocumentId                                     |
| `label`            | `string \| undefined` | No       | Optional link label (e.g., 'references', 'extends'). |
| `createdAt`        | `number`              | Yes      | createdAt                                            |

## CreateDocLinkParams

Parameters for creating a document link.

| Property           | Type                  | Required | Description      |
| ------------------ | --------------------- | -------- | ---------------- |
| `sourceDocumentId` | `string`              | Yes      | sourceDocumentId |
| `targetDocumentId` | `string`              | Yes      | targetDocumentId |
| `label`            | `string \| undefined` | No       | label            |

## GraphResult

Global knowledge graph result.

| Property | Type                                                            | Required | Description |
| -------- | --------------------------------------------------------------- | -------- | ----------- |
| `nodes`  | `{ id: string; slug: string; title: string; state: string; }[]` | Yes      | nodes       |
| `edges`  | `{ source: string; target: string; label?: string; }[]`         | No       | edges       |

## Webhook

A webhook registration.

| Property    | Type       | Required | Description                |
| ----------- | ---------- | -------- | -------------------------- |
| `id`        | `string`   | Yes      | id                         |
| `ownerId`   | `string`   | Yes      | ownerId                    |
| `url`       | `string`   | Yes      | url                        |
| `secret`    | `string`   | Yes      | HMAC signing secret (hex). |
| `events`    | `string[]` | Yes      | Subscribed event types.    |
| `enabled`   | `boolean`  | Yes      | enabled                    |
| `createdAt` | `number`   | Yes      | createdAt                  |
| `updatedAt` | `number`   | Yes      | updatedAt                  |

## CreateWebhookParams

Parameters for creating a webhook.

| Property  | Type                  | Required | Description |
| --------- | --------------------- | -------- | ----------- |
| `ownerId` | `string`              | Yes      | ownerId     |
| `url`     | `string`              | Yes      | url         |
| `secret`  | `string \| undefined` | No       | secret      |
| `events`  | `string[]`            | Yes      | events      |

## WebhookTestResult

Result of a webhook test delivery.

| Property       | Type                  | Required | Description  |
| -------------- | --------------------- | -------- | ------------ |
| `webhookId`    | `string`              | Yes      | webhookId    |
| `delivered`    | `boolean`             | Yes      | delivered    |
| `statusCode`   | `number \| undefined` | No       | statusCode   |
| `responseBody` | `string \| undefined` | No       | responseBody |
| `durationMs`   | `number`              | Yes      | durationMs   |

## SignedUrl

A time-limited signed access token for a document.

| Property     | Type                | Required | Description                                           |
| ------------ | ------------------- | -------- | ----------------------------------------------------- |
| `token`      | `string`            | Yes      | token                                                 |
| `documentId` | `string`            | Yes      | documentId                                            |
| `expiresAt`  | `number`            | Yes      | Expiry timestamp (ms since epoch). 0 = never expires. |
| `permission` | `"read" \| "write"` | Yes      | Permission granted by this token.                     |
| `createdAt`  | `number`            | Yes      | createdAt                                             |

## CreateSignedUrlParams

Parameters for creating a signed URL.

| Property     | Type                             | Required | Description                                                   |
| ------------ | -------------------------------- | -------- | ------------------------------------------------------------- |
| `documentId` | `string`                         | Yes      | documentId                                                    |
| `ttlMs`      | `number \| undefined`            | No       | TTL in milliseconds. 0 = never expires. Defaults to 24 hours. |
| `permission` | `"read" \| "write" \| undefined` | No       | permission                                                    |

## DocumentVisibility

Document visibility level.

```typescript
DocumentVisibility
```

## RoleGrant

A role grant entry.

| Property    | Type                                            | Required | Description |
| ----------- | ----------------------------------------------- | -------- | ----------- |
| `userId`    | `string`                                        | Yes      | userId      |
| `role`      | `"owner" \| "editor" \| "viewer" \| "approver"` | Yes      | role        |
| `grantedAt` | `number`                                        | Yes      | grantedAt   |

## AccessControlList

The full access control list for a document.

| Property     | Type                 | Required | Description |
| ------------ | -------------------- | -------- | ----------- |
| `documentId` | `string`             | Yes      | documentId  |
| `visibility` | `DocumentVisibility` | Yes      | visibility  |
| `grants`     | `RoleGrant[]`        | Yes      | grants      |

## GrantAccessParams

Parameters for granting access.

| Property | Type                                            | Required | Description |
| -------- | ----------------------------------------------- | -------- | ----------- |
| `userId` | `string`                                        | Yes      | userId      |
| `role`   | `"owner" \| "editor" \| "viewer" \| "approver"` | Yes      | role        |

## Organization

An organization grouping users and documents.

| Property    | Type     | Required | Description |
| ----------- | -------- | -------- | ----------- |
| `id`        | `string` | Yes      | id          |
| `slug`      | `string` | Yes      | slug        |
| `name`      | `string` | Yes      | name        |
| `ownerId`   | `string` | Yes      | ownerId     |
| `createdAt` | `number` | Yes      | createdAt   |
| `updatedAt` | `number` | Yes      | updatedAt   |

## CreateOrgParams

Parameters for creating an organization.

| Property  | Type                  | Required | Description |
| --------- | --------------------- | -------- | ----------- |
| `name`    | `string`              | Yes      | name        |
| `ownerId` | `string`              | Yes      | ownerId     |
| `slug`    | `string \| undefined` | No       | slug        |

## ApiKey

An API key record (secret never returned after creation).

| Property    | Type                  | Required | Description                                                            |
| ----------- | --------------------- | -------- | ---------------------------------------------------------------------- |
| `id`        | `string`              | Yes      | id                                                                     |
| `userId`    | `string`              | Yes      | userId                                                                 |
| `name`      | `string`              | Yes      | name                                                                   |
| `prefix`    | `string`              | Yes      | Key prefix for display (first 8 chars of hash). Never the full secret. |
| `createdAt` | `number`              | Yes      | createdAt                                                              |
| `revokedAt` | `number \| undefined` | No       | If set, this key has been revoked.                                     |

## CreateApiKeyParams

Parameters for creating an API key.

| Property | Type     | Required | Description |
| -------- | -------- | -------- | ----------- |
| `userId` | `string` | Yes      | userId      |
| `name`   | `string` | Yes      | name        |

## ApiKeyWithSecret

Full API key including secret — only returned at creation time.

| Property | Type     | Required | Description                                                      |
| -------- | -------- | -------- | ---------------------------------------------------------------- |
| `secret` | `string` | Yes      | The full API key secret. Shown ONCE at creation; store securely. |

## ApprovalChainEntry

An entry in the tamper-evident approval chain.

| Property        | Type                       | Required | Description   |
| --------------- | -------------------------- | -------- | ------------- |
| `approvalId`    | `string`                   | Yes      | approvalId    |
| `reviewerId`    | `string`                   | Yes      | reviewerId    |
| `status`        | `"APPROVED" \| "REJECTED"` | Yes      | status        |
| `atVersion`     | `number`                   | Yes      | atVersion     |
| `timestamp`     | `number`                   | Yes      | timestamp     |
| `chainHash`     | `string`                   | Yes      | chainHash     |
| `prevChainHash` | `string \| null`           | Yes      | prevChainHash |
| `sigHex`        | `string \| undefined`      | No       | sigHex        |

## ApprovalChainResult

Result of verifying the approval chain for a document.

| Property         | Type                   | Required | Description    |
| ---------------- | ---------------------- | -------- | -------------- |
| `valid`          | `boolean`              | Yes      | valid          |
| `length`         | `number`               | Yes      | length         |
| `firstInvalidAt` | `number \| null`       | Yes      | firstInvalidAt |
| `entries`        | `ApprovalChainEntry[]` | Yes      | entries        |

## ContributorOps

Contributor attribution operations.

| Property           | Type                                                   | Required | Description                                                                                                                                 |
| ------------------ | ------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `listContributors` | `(documentId: string) => Promise<ContributorRecord[]>` | Yes      | List all contributors for a document, ordered by net token contribution. MUST return an empty array (not throw) when no contributors exist. |

## BftOps

BFT consensus chain operations.

| Property           | Type                                                   | Required | Description                                                                                                                                                                                   |
| ------------------ | ------------------------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `getApprovalChain` | `(documentId: string) => Promise<ApprovalChainResult>` | Yes      | Retrieve the tamper-evident approval chain for a document. MUST verify each chain link and report the first invalid position. MUST return an empty chain (not throw) when no approvals exist. |

## CollectionOps

Collection CRUD and membership operations.

| Property                  | Type                                                                                 | Required | Description                                                                                                                 |
| ------------------------- | ------------------------------------------------------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------- |
| `createCollection`        | `(params: CreateCollectionParams) => Promise<Collection>`                            | Yes      | Create a new collection. MUST generate a unique slug from the name if not provided.                                         |
| `getCollection`           | `(slug: string) => Promise<Collection \| null>`                                      | Yes      | Retrieve a collection by slug. MUST return null (not throw) when the collection does not exist.                             |
| `listCollections`         | `(params?: ListCollectionsParams) => Promise<ListResult<Collection>>`                | No       | List collections with optional owner filter and pagination.                                                                 |
| `addDocToCollection`      | `(collectionSlug: string, documentSlug: string, position?: number) => Promise<void>` | No       | Add a document to a collection at an optional position. MUST be idempotent — adding the same document twice MUST NOT error. |
| `removeDocFromCollection` | `(collectionSlug: string, documentSlug: string) => Promise<boolean>`                 | Yes      | Remove a document from a collection. MUST return false (not throw) when the document is not in the collection.              |
| `reorderCollection`       | `(collectionSlug: string, orderedSlugs: string[]) => Promise<void>`                  | Yes      | Reorder documents in a collection. MUST accept the full ordered list of document slugs as the new order.                    |
| `exportCollection`        | `(collectionSlug: string) => Promise<CollectionExport>`                              | Yes      | Export a collection with all its documents' latest versions.                                                                |

## CrossDocOps

Cross-document link and graph operations.

| Property             | Type                                                        | Required | Description                                                                                                |
| -------------------- | ----------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------- |
| `createDocumentLink` | `(params: CreateDocLinkParams) => Promise<DocumentLink>`    | Yes      | Create a directed link between two documents. MUST be idempotent — duplicate links SHOULD be deduplicated. |
| `getDocumentLinks`   | `(documentId: string) => Promise<DocumentLink[]>`           | Yes      | Get all links originating from or pointing to a document.                                                  |
| `deleteDocumentLink` | `(documentId: string, linkId: string) => Promise<boolean>`  | Yes      | Delete a document link. MUST return false (not throw) if the link does not exist.                          |
| `getGlobalGraph`     | `(params?: { maxNodes?: number; }) => Promise<GraphResult>` | No       | Get the global document knowledge graph. SHOULD limit results for large backends (e.g., max 500 nodes).    |

## WebhookOps

Webhook registration and delivery operations.

| Property        | Type                                                | Required | Description                                                                                                                            |
| --------------- | --------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `createWebhook` | `(params: CreateWebhookParams) => Promise<Webhook>` | Yes      | Register a new webhook endpoint. MUST generate a signing secret if not provided.                                                       |
| `listWebhooks`  | `(userId: string) => Promise<Webhook[]>`            | Yes      | List webhooks owned by a user.                                                                                                         |
| `deleteWebhook` | `(id: string, userId: string) => Promise<boolean>`  | Yes      | Delete a webhook. MUST verify ownership before deletion. MUST return false (not throw) if the webhook does not exist.                  |
| `testWebhook`   | `(id: string) => Promise<WebhookTestResult>`        | Yes      | Send a synthetic test delivery to a webhook. MUST deliver an HTTP POST to the webhook URL. SHOULD return the response status and body. |

## SignedUrlOps

Signed URL (time-limited access token) operations.

| Property          | Type                                                                                         | Required | Description                                                                                                                         |
| ----------------- | -------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `createSignedUrl` | `(params: CreateSignedUrlParams) => Promise<SignedUrl>`                                      | Yes      | Create a time-limited signed access token for a document. ttlMs=0 means never expires. Default TTL is 24 hours.                     |
| `verifySignedUrl` | `(token: string) => Promise<{ documentId: string; permission: "read" \| "write"; } \| null>` | Yes      | Verify a signed URL token and return the associated document and permission. MUST return null when the token is invalid or expired. |

## AccessControlOps

Document access control operations.

| Property                | Type                                                                    | Required | Description                                                                                                |
| ----------------------- | ----------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------- |
| `getDocumentAccess`     | `(documentId: string) => Promise<AccessControlList>`                    | Yes      | Get the full access control list (visibility + role grants) for a document.                                |
| `grantDocumentAccess`   | `(documentId: string, params: GrantAccessParams) => Promise<void>`      | Yes      | Grant a user a role on a document. MUST be idempotent — granting the same role twice MUST NOT error.       |
| `revokeDocumentAccess`  | `(documentId: string, userId: string) => Promise<boolean>`              | Yes      | Revoke all role grants for a user on a document. MUST return false (not throw) when the user has no grant. |
| `setDocumentVisibility` | `(documentId: string, visibility: DocumentVisibility) => Promise<void>` | Yes      | Set the document's visibility level.                                                                       |

## OrganizationOps

Organization management operations.

| Property             | Type                                                                | Required | Description                                                                                          |
| -------------------- | ------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------- |
| `createOrganization` | `(params: CreateOrgParams) => Promise<Organization>`                | Yes      | Create a new organization. MUST generate a unique slug from the name if not provided.                |
| `getOrganization`    | `(slug: string) => Promise<Organization \| null>`                   | Yes      | Retrieve an organization by slug. MUST return null (not throw) when the organization does not exist. |
| `listOrganizations`  | `(userId: string) => Promise<Organization[]>`                       | Yes      | List organizations the user belongs to or owns.                                                      |
| `addOrgMember`       | `(orgSlug: string, userId: string, role?: string) => Promise<void>` | No       | Add a user to an organization. MUST be idempotent — adding the same member twice MUST NOT error.     |
| `removeOrgMember`    | `(orgSlug: string, userId: string) => Promise<boolean>`             | Yes      | Remove a user from an organization. MUST return false (not throw) when the user is not a member.     |

## ApiKeyOps

API key management operations.

| Property       | Type                                                        | Required | Description                                                                                                                                                     |
| -------------- | ----------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `createApiKey` | `(params: CreateApiKeyParams) => Promise<ApiKeyWithSecret>` | Yes      | Create a new API key for a user. MUST return the full secret exactly once in ApiKeyWithSecret. Subsequent retrieval via listApiKeys MUST NOT return the secret. |
| `listApiKeys`  | `(userId: string) => Promise<ApiKey[]>`                     | Yes      | List all active API keys for a user (without secrets).                                                                                                          |
| `deleteApiKey` | `(id: string, userId: string) => Promise<boolean>`          | Yes      | Revoke an API key. MUST verify ownership before revoking. MUST return false (not throw) if the key does not exist.                                              |
| `rotateApiKey` | `(id: string, userId: string) => Promise<ApiKeyWithSecret>` | Yes      | Rotate an API key: revoke the old one and issue a new one with the same name. MUST verify ownership before rotation.                                            |

## Backend

Backend — the complete LLMtxt persistence and coordination interface.  Implementations MUST satisfy all sub-interfaces. Consumers of this interface SHOULD NOT depend on any implementation-specific methods.  Both LocalBackend (packages/llmtxt/src/local/) and RemoteBackend (packages/llmtxt/src/remote/) implement this interface.

| Property | Type                  | Required | Description                                                                                                                                                      |
| -------- | --------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `open`   | `() => Promise<void>` | Yes      | Open the backend connection / apply migrations. MUST be called before any other method. MUST be idempotent (calling open twice MUST NOT error).                  |
| `close`  | `() => Promise<void>` | Yes      | Close the backend, releasing resources (DB handles, timers, sockets). MUST stop all background reapers and interval timers. MUST be safe to call multiple times. |
| `config` | `BackendConfig`       | Yes      | The BackendConfig this instance was constructed with.                                                                                                            |

## ExportErrorCode

Error codes for the document export subsystem.

```typescript
ExportErrorCode
```

## ExportFormat

Supported document export formats.

```typescript
ExportFormat
```

## ExportDocumentParams

Parameters for exporting a single document.

| Property          | Type                   | Required | Description                                                                       |
| ----------------- | ---------------------- | -------- | --------------------------------------------------------------------------------- |
| `slug`            | `string`               | Yes      | URL-safe document slug.                                                           |
| `format`          | `ExportFormat`         | Yes      | Export format.                                                                    |
| `outputPath`      | `string`               | Yes      | Absolute or relative path to write the output file.                               |
| `includeMetadata` | `boolean \| undefined` | No       | Whether to include metadata (frontmatter/structured fields). Default true.        |
| `sign`            | `boolean \| undefined` | No       | If true, sign the export manifest with the local Ed25519 identity. Default false. |

## ExportDocumentResult

Result returned by exportDocument().

| Property       | Type             | Required | Description                                                        |
| -------------- | ---------------- | -------- | ------------------------------------------------------------------ |
| `filePath`     | `string`         | Yes      | Absolute path of the written file.                                 |
| `slug`         | `string`         | Yes      | Slug of the exported document.                                     |
| `version`      | `number`         | Yes      | Version number exported.                                           |
| `fileHash`     | `string`         | Yes      | SHA-256 hex of the written file bytes.                             |
| `byteCount`    | `number`         | Yes      | Number of bytes written.                                           |
| `exportedAt`   | `string`         | Yes      | ISO 8601 UTC timestamp of export.                                  |
| `signatureHex` | `string \| null` | Yes      | Ed25519 signature hex over fileHash, if sign=true. Null otherwise. |

## ExportAllParams

Parameters for exporting all documents to a directory.

| Property          | Type                   | Required | Description                                                                 |
| ----------------- | ---------------------- | -------- | --------------------------------------------------------------------------- |
| `format`          | `ExportFormat`         | Yes      | format                                                                      |
| `outputDir`       | `string`               | Yes      | Directory to write files into. One file per document, named `<slug>.<ext>`. |
| `state`           | `string \| undefined`  | No       | Filter by lifecycle state. If absent, exports all documents.                |
| `includeMetadata` | `boolean \| undefined` | No       | includeMetadata                                                             |
| `sign`            | `boolean \| undefined` | No       | sign                                                                        |

## ExportAllResult

Result returned by exportAll().

| Property      | Type                                  | Required | Description |
| ------------- | ------------------------------------- | -------- | ----------- |
| `exported`    | `ExportDocumentResult[]`              | Yes      | exported    |
| `skipped`     | `{ slug: string; reason: string; }[]` | Yes      | skipped     |
| `totalCount`  | `number`                              | Yes      | totalCount  |
| `failedCount` | `number`                              | Yes      | failedCount |

## ImportDocumentParams

Parameters for importing a document from a file.

| Property     | Type                                     | Required | Description                                                                                                                                                                                                               |
| ------------ | ---------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `filePath`   | `string`                                 | Yes      | Path to the file to import (.md, .json, .txt, .llmtxt).                                                                                                                                                                   |
| `importedBy` | `string`                                 | Yes      | Agent performing the import.                                                                                                                                                                                              |
| `onConflict` | `"new_version" \| "create" \| undefined` | No       | Conflict strategy when a document with the same slug already exists. 'new\_version': publish the imported content as a new version (default). 'create': fail with ExportError('SLUG\_EXISTS') if the slug already exists. |

## ImportDocumentResult

Result returned by importDocument().

| Property        | Type                              | Required | Description                                                   |
| --------------- | --------------------------------- | -------- | ------------------------------------------------------------- |
| `action`        | `"created" \| "version_appended"` | Yes      | Whether a new document was created or a version was appended. |
| `slug`          | `string`                          | Yes      | slug                                                          |
| `documentId`    | `string`                          | Yes      | documentId                                                    |
| `versionNumber` | `number`                          | Yes      | versionNumber                                                 |
| `contentHash`   | `string`                          | Yes      | contentHash                                                   |

## ExportOps

Document export operations.

| Property          | Type                                                              | Required | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------- | ----------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `exportDocument`  | `(params: ExportDocumentParams) => Promise<ExportDocumentResult>` | Yes      | Export a single document to a file on disk.  MUST resolve slug to a document; MUST throw ExportError('DOC\_NOT\_FOUND') if absent. MUST fetch the latest version content from the backend. MUST write the file atomically (write to .tmp then rename). MUST return the SHA-256 hash of the written bytes. MUST create intermediate directories via mkdirSync recursive. MUST NOT mutate any document, version, or event row in the database.                                                                             |
| `exportAll`       | `(params: ExportAllParams) => Promise<ExportAllResult>`           | Yes      | Export all documents to a directory.  MUST iterate all documents (paginating via listDocuments). MUST call exportDocument for each; individual failures MUST be collected in skipped, not thrown. MUST write each file as . inside outputDir.                                                                                                                                                                                                                                                                            |
| `importDocument`  | `(params: ImportDocumentParams) => Promise<ImportDocumentResult>` | Yes      | Import a document from a file on disk.  MUST parse frontmatter from .md and .llmtxt files. MUST parse the `content` field from .json files. MUST read raw body from .txt files (no frontmatter). MUST NOT silently ignore a frontmatter content\_hash that does not match the body. MUST create a new document if no document with the slug exists. MUST publish a new version if the document exists and onConflict='new\_version'. MUST return ExportError('SLUG\_EXISTS') if document exists and onConflict='create'. |
| `getChangesSince` | `(dbVersion: bigint) => Promise<Uint8Array>`                      | Yes      | Returns all changes made to this database since `dbVersion`.  `dbVersion = 0n` returns the full change history. Returns an empty Uint8Array (not null) when no changes exist.  The changeset is the cr-sqlite binary wire format as defined in docs/specs/P2-cr-sqlite.md §3.3 (DR-P2-03).                                                                                                                                                                                                                               |
| `applyChanges`    | `(changeset: Uint8Array) => Promise<bigint>`                      | Yes      | Applies a changeset received from a peer.  MUST be idempotent: applying the same changeset twice produces identical state.  For rows affecting section\_crdt\_states.crdt\_state, the implementation MUST perform application-level Loro merge instead of cr-sqlite LWW (DR-P2-04).                                                                                                                                                                                                                                      |

## FrontmatterMeta

Structured input for `canonicalFrontmatter`.  Mirrors `FrontmatterMeta` in `crates/llmtxt-core/src/canonical.rs`. Contributors are sorted inside this function — callers MUST NOT pre-sort.

| Property       | Type       | Required | Description                                            |
| -------------- | ---------- | -------- | ------------------------------------------------------ |
| `title`        | `string`   | Yes      | Document title (UTF-8, double-quoted in output).       |
| `slug`         | `string`   | Yes      | URL-safe slug.                                         |
| `version`      | `number`   | Yes      | Integer version number of the exported state.          |
| `state`        | `string`   | Yes      | Lifecycle state string (e.g. "DRAFT", "APPROVED").     |
| `contributors` | `string[]` | Yes      | Agent IDs — sorted lexicographically by this function. |
| `content_hash` | `string`   | Yes      | SHA-256 hex of the body content (64 lowercase chars).  |
| `exported_at`  | `string`   | Yes      | ISO 8601 UTC timestamp with millisecond precision.     |

## DocumentExportState

Self-contained document snapshot passed to all format serializers.  All fields required by the canonical frontmatter schema are present at the top level; additional fields feed the JSON format.

| Property       | Type                            | Required | Description                                                                                                                        |
| -------------- | ------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `title`        | `string`                        | Yes      | Human-readable document title.                                                                                                     |
| `slug`         | `string`                        | Yes      | URL-safe slug.                                                                                                                     |
| `version`      | `number`                        | Yes      | Version number of the exported state (integer, 1-based).                                                                           |
| `state`        | `string`                        | Yes      | Lifecycle state string (DRAFT \| REVIEW \| LOCKED \| ARCHIVED).                                                                    |
| `contributors` | `string[]`                      | Yes      | Agent IDs that have contributed to this document. Formatters MUST sort these lexicographically before serialization.               |
| `contentHash`  | `string`                        | Yes      | SHA-256 hex of the body content (64 lowercase hex chars). Callers compute this; formatters embed it verbatim.                      |
| `exportedAt`   | `string`                        | Yes      | ISO 8601 UTC timestamp with millisecond precision. Injected by the caller so that determinism is achievable across repeated calls. |
| `content`      | `string`                        | Yes      | Full body content of the exported version.                                                                                         |
| `labels`       | `string[] \| null \| undefined` | No       | Arbitrary metadata labels. Present if available; omitted or null otherwise.                                                        |
| `createdBy`    | `string \| null \| undefined`   | No       | Agent that created the document.                                                                                                   |
| `createdAt`    | `number \| null \| undefined`   | No       | Creation timestamp (Unix milliseconds).                                                                                            |
| `updatedAt`    | `number \| null \| undefined`   | No       | Last-modified timestamp (Unix milliseconds).                                                                                       |
| `versionCount` | `number \| null \| undefined`   | No       | Total number of versions for this document.                                                                                        |
| `chainRef`     | `string \| null \| undefined`   | No       | BFT approval chain hash from `getApprovalChain`. Null when no approvals exist or when CRDT state is unavailable (T451 stub).       |

## ExportOpts

Options controlling format serializer behaviour.  All fields are optional. Formatters use sensible defaults when absent.

| Property          | Type                   | Required | Description                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------- | ---------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `includeMetadata` | `boolean \| undefined` | No       | Whether to include metadata (frontmatter / structured fields). Defaults to `true`.  When `false`: - `formatMarkdown` emits body only (no frontmatter fences). - `formatLlmtxt` behaves identically to `formatMarkdown` with `includeMetadata: false`. - `formatJson` ignores this flag (JSON format always includes metadata). - `formatTxt` ignores this flag (plain-text format never includes metadata). |

## ParsedImport

| Property              | Type             | Required | Description                                                                                                                    |
| --------------------- | ---------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `slug`                | `string`         | Yes      | URL-safe slug (from frontmatter, or derived from filename).                                                                    |
| `title`               | `string`         | Yes      | Human-readable title (from frontmatter, or derived from filename).                                                             |
| `content`             | `string`         | Yes      | Raw body content.                                                                                                              |
| `expectedContentHash` | `string \| null` | Yes      | Expected SHA-256 hex of content, if present in frontmatter. Callers MUST verify this matches the actual body before importing. |

## Document

```typescript
{ slug: string; expiresAt: number | null; createdAt: number; id: string; title: string; state: string; createdBy: string; visibility: string; updatedAt: number; versionCount: number; labelsJson: string; eventSeqCounter: number; bftF: number; requiredApprovals: number; approvalTimeoutMs: number; }
```

## NewDocument

```typescript
{ slug: string; createdAt: number; id: string; title: string; createdBy: string; updatedAt: number; expiresAt?: number | null | undefined; state?: string | undefined; visibility?: string | undefined; versionCount?: number | undefined; labelsJson?: string | undefined; eventSeqCounter?: number | undefined; bftF?: number | undefined; requiredApprovals?: number | undefined; approvalTimeoutMs?: number | undefined; }
```

## Version

```typescript
{ versionNumber: number; changelog: string | null; createdAt: number; id: string; createdBy: string | null; documentId: string; compressedData: unknown; contentHash: string; tokenCount: number | null; patchText: string | null; baseVersion: number | null; storageType: string; storageKey: string | null; }
```

## NewVersion

```typescript
{ versionNumber: number; createdAt: number; id: string; documentId: string; contentHash: string; changelog?: string | null | undefined; createdBy?: string | null | undefined; compressedData?: unknown; tokenCount?: number | null | undefined; patchText?: string | null | undefined; baseVersion?: number | null | undefined; storageType?: string | undefined; storageKey?: string | null | undefined; }
```

## StateTransition

```typescript
{ id: string; documentId: string; fromState: string; toState: string; changedBy: string; changedAt: number; reason: string | null; atVersion: number; }
```

## NewStateTransition

```typescript
{ id: string; documentId: string; fromState: string; toState: string; changedBy: string; changedAt: number; atVersion: number; reason?: string | null | undefined; }
```

## Approval

```typescript
{ timestamp: number; id: string; bftF: number; documentId: string; reason: string | null; atVersion: number; reviewerId: string; status: string; sigHex: string | null; canonicalPayload: string | null; chainHash: string | null; prevChainHash: string | null; }
```

## NewApproval

```typescript
{ timestamp: number; id: string; documentId: string; atVersion: number; reviewerId: string; status: string; bftF?: number | undefined; reason?: string | null | undefined; sigHex?: string | null | undefined; canonicalPayload?: string | null | undefined; chainHash?: string | null | undefined; prevChainHash?: string | null | undefined; }
```

## SectionCrdtState

```typescript
{ updatedAt: number; documentId: string; sectionId: string; clock: number; crdtState: unknown; }
```

## NewSectionCrdtState

```typescript
{ updatedAt: number; documentId: string; sectionId: string; crdtState: unknown; clock?: number | undefined; }
```

## SectionCrdtUpdate

```typescript
{ createdAt: number; id: string; documentId: string; sectionId: string; updateBlob: unknown; clientId: string; seq: number; }
```

## NewSectionCrdtUpdate

```typescript
{ createdAt: number; id: string; documentId: string; sectionId: string; updateBlob: unknown; clientId: string; seq: number; }
```

## DocumentEvent

```typescript
{ createdAt: number; id: string; documentId: string; seq: number; eventType: string; actorId: string; payloadJson: string; idempotencyKey: string | null; prevHash: string | null; }
```

## NewDocumentEvent

```typescript
{ createdAt: number; id: string; documentId: string; seq: number; eventType: string; actorId: string; payloadJson?: string | undefined; idempotencyKey?: string | null | undefined; prevHash?: string | null | undefined; }
```

## AgentPubkey

```typescript
{ agentId: string; createdAt: number; id: string; pubkeyHex: string; label: string | null; revokedAt: number | null; }
```

## NewAgentPubkey

```typescript
{ agentId: string; createdAt: number; id: string; pubkeyHex: string; label?: string | null | undefined; revokedAt?: number | null | undefined; }
```

## AgentSignatureNonce

```typescript
{ expiresAt: number; agentId: string; nonce: string; firstSeen: number; }
```

## NewAgentSignatureNonce

```typescript
{ expiresAt: number; agentId: string; nonce: string; firstSeen: number; }
```

## SectionLease

```typescript
{ expiresAt: number; id: string; resource: string; holder: string; acquiredAt: number; }
```

## NewSectionLease

```typescript
{ expiresAt: number; id: string; resource: string; holder: string; acquiredAt: number; }
```

## AgentInboxMessage

```typescript
{ createdAt: number; id: string; toAgentId: string; envelopeJson: string; exp: number; }
```

## NewAgentInboxMessage

```typescript
{ createdAt: number; id: string; toAgentId: string; envelopeJson: string; exp: number; }
```

## ScratchpadEntry

```typescript
{ createdAt: number; id: string; payloadJson: string; toAgentId: string; exp: number; fromAgentId: string; }
```

## NewScratchpadEntry

```typescript
{ createdAt: number; id: string; toAgentId: string; exp: number; fromAgentId: string; payloadJson?: string | undefined; }
```

## SectionEmbedding

```typescript
{ versionNumber: number; createdAt: number; id: string; documentId: string; sectionKey: string; embeddingBlob: unknown; dimensions: number; modelId: string; }
```

## NewSectionEmbedding

```typescript
{ versionNumber: number; createdAt: number; id: string; documentId: string; embeddingBlob: unknown; dimensions: number; modelId: string; sectionKey?: string | undefined; }
```

## BlobAttachment

```typescript
{ id: string; docSlug: string; blobName: string; hash: string; size: number; contentType: string; uploadedBy: string; uploadedAt: number; deletedAt: number | null; }
```

## NewBlobAttachment

```typescript
{ id: string; docSlug: string; blobName: string; hash: string; size: number; contentType: string; uploadedBy: string; uploadedAt: number; deletedAt?: number | null | undefined; }
```

## InsertDocument

```typescript
{ id: string; slug: string; title: string; createdBy: string; createdAt: number; updatedAt: number; state?: string | undefined; visibility?: string | undefined; versionCount?: number | undefined; labelsJson?: string | undefined; expiresAt?: number | null | undefined; eventSeqCounter?: number | undefined; bftF?: number | undefined; requiredApprovals?: number | undefined; approvalTimeoutMs?: number | undefined; }
```

## SelectDocument

```typescript
{ id: string; slug: string; title: string; state: string; createdBy: string; visibility: string; createdAt: number; updatedAt: number; versionCount: number; labelsJson: string; expiresAt: number | null; eventSeqCounter: number; bftF: number; requiredApprovals: number; approvalTimeoutMs: number; }
```

## PeerTransport

PeerTransport — transport abstraction for P2P mesh (P3 spec §4.1).  Implementations MUST complete Ed25519 mutual handshake before any changeset data is exchanged. Unauthenticated peers MUST be rejected.

| Property        | Type                                                                              | Required | Description                                                                                                                                                                                                                     |
| --------------- | --------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `type`          | `string`                                                                          | Yes      | Transport type identifier (e.g., `"unix"`, `"http"`).                                                                                                                                                                           |
| `listen`        | `(onChangeset: (peerId: string, changeset: Uint8Array) => void) => Promise<void>` | Yes      | Listen for incoming connections.  MUST complete Ed25519 mutual handshake before calling `onChangeset()`. MUST reject connections that fail the handshake. MUST call `onChangeset()` for each received, authenticated changeset. |
| `sendChangeset` | `(peerId: string, peerAddress: string, changeset: Uint8Array) => Promise<void>`   | Yes      | Send a changeset to a specific peer.  MUST complete Ed25519 mutual handshake before sending any data. MUST throw `PeerUnreachableError` if the peer is unreachable after `MAX_RETRIES` attempts.                                |
| `close`         | `() => Promise<void>`                                                             | Yes      | Graceful shutdown — close all open connections and stop listening.                                                                                                                                                              |

## TransportIdentity

Local identity used by the transport layer for handshakes.

| Property     | Type                          | Required | Description                                  |
| ------------ | ----------------------------- | -------- | -------------------------------------------- |
| `agentId`    | `string`                      | Yes      | Hex-encoded SHA-256 of the public key bytes. |
| `publicKey`  | `Uint8Array<ArrayBufferLike>` | Yes      | 32-byte Ed25519 public key.                  |
| `privateKey` | `Uint8Array<ArrayBufferLike>` | Yes      | 32-byte Ed25519 private key seed.            |

## PeerInfo

| Property       | Type     | Required | Description                                                              |
| -------------- | -------- | -------- | ------------------------------------------------------------------------ |
| `agentId`      | `string` | Yes      | Hex-encoded public key hash (agentId).                                   |
| `address`      | `string` | Yes      | Transport address (e.g. "unix:/tmp/agent.sock" or "<http://host:port>"). |
| `pubkeyBase64` | `string` | Yes      | Base64-encoded Ed25519 public key.                                       |

## PeerRegistry

| Property       | Type                        | Required | Description                                                   |
| -------------- | --------------------------- | -------- | ------------------------------------------------------------- |
| `discover`     | `() => Promise<PeerInfo[]>` | Yes      | Returns the current set of discovered peers (excluding self). |
| `markInactive` | `(agentId: string) => void` | Yes      | Mark a peer as temporarily inactive after repeated failures.  |

## PeerSyncState

In-memory peer sync state. Persisted externally via llmtxt\_mesh\_state table.

| Property          | Type                  | Required | Description     |
| ----------------- | --------------------- | -------- | --------------- |
| `lastSyncVersion` | `bigint`              | Yes      | lastSyncVersion |
| `failureCount`    | `number`              | Yes      | failureCount    |
| `lastFailureAt`   | `number \| undefined` | No       | lastFailureAt   |

## SyncEngineOptions

| Property          | Type                  | Required | Description                                                    |
| ----------------- | --------------------- | -------- | -------------------------------------------------------------- |
| `backend`         | `Backend`             | Yes      | backend                                                        |
| `transport`       | `PeerTransport`       | Yes      | transport                                                      |
| `discovery`       | `PeerRegistry`        | Yes      | discovery                                                      |
| `identity`        | `AgentIdentity`       | Yes      | identity                                                       |
| `syncIntervalMs`  | `number \| undefined` | No       | syncIntervalMs                                                 |
| `maxPeerFailures` | `number \| undefined` | No       | Maximum consecutive failures before a peer is marked inactive. |

## A2AEnvelope

A2A message envelope. Spec §7.1:    type, from, to, payload, sig, sentAt   sig = base64-encoded Ed25519 signature over canonical JSON of        type, from, to, payload, sentAt  (fields alphabetically sorted,       no trailing whitespace).

| Property  | Type                      | Required | Description |
| --------- | ------------------------- | -------- | ----------- |
| `type`    | `"a2a"`                   | Yes      | type        |
| `from`    | `string`                  | Yes      | from        |
| `to`      | `string`                  | Yes      | to          |
| `payload` | `Record<string, unknown>` | Yes      | payload     |
| `sig`     | `string`                  | Yes      | sig         |
| `sentAt`  | `string`                  | Yes      | sentAt      |

## MeshMessengerOptions

| Property    | Type                                             | Required | Description                                                               |
| ----------- | ------------------------------------------------ | -------- | ------------------------------------------------------------------------- |
| `identity`  | `AgentIdentity`                                  | Yes      | identity                                                                  |
| `transport` | `PeerTransport`                                  | Yes      | transport                                                                 |
| `discovery` | `PeerRegistry`                                   | Yes      | discovery                                                                 |
| `onMessage` | `((envelope: A2AEnvelope) => void) \| undefined` | No       | Handler called when a valid, verified A2A message arrives for THIS agent. |

## PostgresBackendConfig

Extended config for PostgresBackend.

| Property           | Type                  | Required | Description                                                                                                                                 |
| ------------------ | --------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `connectionString` | `string \| undefined` | No       | PostgreSQL connection string. MUST be in the format: postgresql://user:passhost:5432/dbname Defaults to DATABASE\_URL environment variable. |
| `maxConnections`   | `number \| undefined` | No       | Maximum number of connections in the pool. Defaults to 10.                                                                                  |

## PgBackendHandle

| Property  | Type                  | Required | Description                               |
| --------- | --------------------- | -------- | ----------------------------------------- |
| `adapter` | `PgContractAdapter`   | Yes      | The contract-test-ready adapter.          |
| `cleanup` | `() => Promise<void>` | Yes      | Drop the test schema and end connections. |

## BlobChangeset

Extended changeset type carrying optional blob references.  This wraps the existing cr-sqlite binary changeset (Uint8Array) with a separate `blobs` array that carries manifest metadata — never raw bytes. The blob bytes are pulled lazily on first `getBlob(includeData=true)` call.

| Property         | Type                          | Required | Description                                                        |
| ---------------- | ----------------------------- | -------- | ------------------------------------------------------------------ |
| `crsqlChangeset` | `Uint8Array<ArrayBufferLike>` | Yes      | The cr-sqlite binary changeset (may be empty Uint8Array).          |
| `blobs`          | `BlobRef[]`                   | Yes      | Blob references for all blob operations in the transaction window. |

## ApplyBlobChangesetResult

Result of applying a BlobChangeset.

| Property         | Type       | Required | Description                                                  |
| ---------------- | ---------- | -------- | ------------------------------------------------------------ |
| `applied`        | `number`   | Yes      | Number of blob refs successfully applied (winners inserted). |
| `discarded`      | `number`   | Yes      | Number of blob refs that lost LWW and were discarded.        |
| `pendingFetches` | `string[]` | Yes      | Hashes scheduled for lazy background pull.                   |

## BlobRefWithDocSlug

Extended BlobRef used internally in the sync layer. docSlug is required for manifest writes but is omitted from the public BlobRef spec interface (which is scoped to a document context by the caller).

| Property  | Type     | Required | Description |
| --------- | -------- | -------- | ----------- |
| `docSlug` | `string` | Yes      | docSlug     |

## PeerRegistration

Raw format of a `.peer` file written by a running agent (P3 spec §3.2).  All fields are required. Peer files missing any field are rejected.

| Property       | Type       | Required | Description                                                                     |
| -------------- | ---------- | -------- | ------------------------------------------------------------------------------- |
| `agentId`      | `string`   | Yes      | Hex-encoded SHA-256 of the agent's Ed25519 public key bytes.                    |
| `transport`    | `string`   | Yes      | Transport address string. Format: `unix:<absolute-path>` or `http://host:port`. |
| `pubkey`       | `string`   | Yes      | Base64-encoded 32-byte Ed25519 public key.                                      |
| `capabilities` | `string[]` | Yes      | Capabilities advertised by this peer.                                           |
| `startedAt`    | `string`   | Yes      | ISO-8601 timestamp when the agent started.                                      |

## PeerInfo

A validated peer — returned from `discover()` and `loadStaticConfig()`. All entries here have been security-checked.

| Property | Type      | Required | Description                                                             |
| -------- | --------- | -------- | ----------------------------------------------------------------------- |
| `active` | `boolean` | Yes      | Whether the peer is considered active (startedAt within PEER\_TTL\_MS). |

## EventPublisher

An object that can publish typed events onto a named channel.  Implementations MUST be synchronous-safe — callers do not await publish().

| Property  | Type                                  | Required | Description                                                                                                                                                           |
| --------- | ------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `publish` | `(channel: string, event: T) => void` | Yes      | Emit `event` on `channel`.  All active subscribers on `channel` MUST receive the event in FIFO order. Delivery to a slow subscriber MUST NOT block other subscribers. |

## EventSubscriber

An object that can return an async iterable of typed events from a channel.  The iterable MUST yield events in the order they were published. Calling `return()` on the iterator MUST unsubscribe from the channel.

| Property    | Type                                    | Required | Description                                                                                                                                                                                                                                               |
| ----------- | --------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `subscribe` | `(channel: string) => AsyncIterable<T>` | Yes      | Return an `AsyncIterable<T>` that yields every event published on `channel` after the subscription is opened.  Past events (before the subscription was opened) MUST NOT be replayed unless the implementation explicitly supports seek/cursor semantics. |

## EventStream

A bidirectional event stream that can both publish and subscribe.

## EmitterLike

Minimal EventEmitter surface required by makeEventStream.

| Property | Type                                                              | Required | Description |
| -------- | ----------------------------------------------------------------- | -------- | ----------- |
| `on`     | `(event: string, listener: (...args: unknown[]) => void) => void` | Yes      | on          |
| `off`    | `(event: string, listener: (...args: unknown[]) => void) => void` | Yes      | off         |

## DocumentEventBusLike

Shape of the document event bus used by PostgresBackend (injected from apps/backend realtime layer).  Uses a single `'document'` channel and emits objects shaped as `{ type, slug, documentId, timestamp, actor, data }`.

| Property | Type                                                                | Required | Description |
| -------- | ------------------------------------------------------------------- | -------- | ----------- |
| `on`     | `(event: "document", listener: (payload: unknown) => void) => void` | Yes      | on          |
| `off`    | `(event: "document", listener: (payload: unknown) => void) => void` | Yes      | off         |

## PresenceEntry

Presence state broadcast by an agent to all connected peers. Matches the JSON structure in spec §6.1.

| Property     | Type             | Required | Description                                                       |
| ------------ | ---------------- | -------- | ----------------------------------------------------------------- |
| `agentId`    | `string`         | Yes      | Hex-encoded public key hash.                                      |
| `documentId` | `string \| null` | Yes      | Document the agent is currently editing (null if idle).           |
| `sectionId`  | `string \| null` | Yes      | Section within the document (null if not section-level).          |
| `updatedAt`  | `string`         | Yes      | ISO-8601 timestamp of last update.                                |
| `ttl`        | `number`         | Yes      | Time-to-live in seconds. Entries expire after this many seconds.  |
| `receivedAt` | `number`         | Yes      | Wall-clock epoch ms when this entry was received/created locally. |

## PresenceManagerOptions

| Property              | Type                          | Required | Description                                                              |
| --------------------- | ----------------------------- | -------- | ------------------------------------------------------------------------ |
| `agentId`             | `string`                      | Yes      | agentId                                                                  |
| `transport`           | `PeerTransport`               | Yes      | transport                                                                |
| `discovery`           | `PeerRegistry`                | Yes      | discovery                                                                |
| `ttlSeconds`          | `number \| undefined`         | No       | Default TTL in seconds (spec default: 30).                               |
| `broadcastIntervalMs` | `number \| undefined`         | No       | Broadcast interval in ms (spec: 10 s).                                   |
| `rateLimitWindowMs`   | `number \| undefined`         | No       | Rate limit window in ms — max 1 message per peer per window (spec: 5 s). |
| `initialDocumentId`   | `string \| null \| undefined` | No       | Current document/section the agent is editing (updated via setPresence). |
| `initialSectionId`    | `string \| null \| undefined` | No       | initialSectionId                                                         |

## AgentSessionState

Session state machine: Idle - Open - Active - Closing - Closed  - Idle: Initial state, waiting for open() - Open: Backend initialization in progress (transient; transitions to Active) - Active: Ready for contributions via contribute() - Closing: Teardown in progress (mutex-protected) - Closed: Teardown complete, receipt emitted

```typescript
AgentSessionState
```

## ContributionReceipt

Contribution Receipt: auditable proof of work performed during a session.  RFC 2119 requirement: All fields are mandatory except signature (which is mandatory only for RemoteBackend).

| Property            | Type                  | Required | Description                                                                                                                                                                                                                                                                                                                                                             |
| ------------------- | --------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sessionId`         | `string`              | Yes      | Session ID (128-bit random, unguessable).                                                                                                                                                                                                                                                                                                                               |
| `agentId`           | `string`              | Yes      | Agent identity ID (must match authenticated identity in backend).                                                                                                                                                                                                                                                                                                       |
| `documentIds`       | `string[]`            | Yes      | Unique document IDs written during the session (sorted for determinism).                                                                                                                                                                                                                                                                                                |
| `eventCount`        | `number`              | Yes      | Total successful write operations performed via contribute().                                                                                                                                                                                                                                                                                                           |
| `sessionDurationMs` | `number`              | Yes      | Session duration in milliseconds (closedAt - openedAt).                                                                                                                                                                                                                                                                                                                 |
| `openedAt`          | `string`              | Yes      | ISO 8601 UTC timestamp of session open.                                                                                                                                                                                                                                                                                                                                 |
| `closedAt`          | `string`              | Yes      | ISO 8601 UTC timestamp of session close.                                                                                                                                                                                                                                                                                                                                |
| `signature`         | `string \| undefined` | No       | Ed25519 signature over the canonical receipt payload. MUST be present when backend is RemoteBackend (cross-network). MAY be omitted for LocalBackend (same-process).  Signature covers: SHA-256(sessionId + agentId + documentIds.sort().join(',') +                   eventCount + openedAt + closedAt)  Stub for now — T461 will add Ed25519 signing to AgentSession. |

## CloseStepError

Error raised when one or more close() teardown steps fail while the session still reaches the Closed state. Callers can inspect `errors` for details and `receipt` for the partial receipt.

| Property | Type     | Required | Description |
| -------- | -------- | -------- | ----------- |
| `step`   | `string` | Yes      | step        |
| `error`  | `Error`  | Yes      | error       |

## AgentSessionOptions

AgentSessionOptions: constructor configuration.

| Property    | Type                  | Required | Description                                                                                                                                                                                    |
| ----------- | --------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `backend`   | `Backend`             | Yes      | Backend to operate through (LocalBackend or RemoteBackend). Typed as Backend to enable proper method calls; consumers pass concrete implementations (LocalBackend or RemoteBackend).           |
| `agentId`   | `string`              | Yes      | Agent identity. MUST match the authenticated identity registered in the backend's identity primitives.                                                                                         |
| `sessionId` | `string \| undefined` | No       | Cryptographically random session ID (128-bit entropy minimum). If omitted, AgentSession generates one using crypto.randomUUID(). MUST be unguessable; predictable IDs allow session hijacking. |
| `label`     | `string \| undefined` | No       | Human-readable label for this session. Used in receipts. Defaults to agentId + timestamp ISO string.                                                                                           |

## PostgresRowChange

A single row-level change captured from Postgres.  In a full implementation this would be populated via Postgres LISTEN/NOTIFY on a changes table, or via logical decoding (pg\_logical\_emit\_message).

| Property    | Type                               | Required | Description                                                      |
| ----------- | ---------------------------------- | -------- | ---------------------------------------------------------------- |
| `table`     | `string`                           | Yes      | Name of the table that changed (e.g., "documents", "versions").  |
| `op`        | `"INSERT" \| "UPDATE" \| "DELETE"` | Yes      | Operation type.                                                  |
| `newRow`    | `Record<string, unknown> \| null`  | Yes      | The row data after the change (null for DELETE).                 |
| `oldRow`    | `Record<string, unknown> \| null`  | Yes      | The row data before the change (null for INSERT).                |
| `txid`      | `number`                           | Yes      | Postgres transaction ID (xid) — used as a logical "db\_version". |
| `changedAt` | `string`                           | Yes      | ISO-8601 timestamp of the change.                                |

## PostgresChangeset

Wire format for a PostgresChangeset batch.  TODO: Align with the binary format produced by LocalBackend.getChangesSince() once the cr-sqlite changeset schema is stabilised in P2.6.

| Property  | Type                  | Required | Description                                                              |
| --------- | --------------------- | -------- | ------------------------------------------------------------------------ |
| `txids`   | `number[]`            | Yes      | Postgres transaction IDs included in this batch.                         |
| `changes` | `PostgresRowChange[]` | Yes      | Serialized row changes (JSON for now; binary in full implementation).    |
| `maxTxid` | `number`              | Yes      | The highest txid in this batch (used as "sinceVersion" by the receiver). |

## PostgresChangesetAdapterOptions

Options for constructing a PostgresChangesetAdapter.

| Property   | Type                  | Required | Description                                                                                                                                                                   |
| ---------- | --------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `db`       | `unknown`             | Yes      | Postgres client or connection pool used for querying row changes.  TODO: Replace `unknown` with the concrete postgres.js client type once the full implementation is written. |
| `sinceXid` | `number \| undefined` | No       | The last synced Postgres transaction ID (xid) for a given peer. Rows with txid  sinceXid will be included in the next changeset. Defaults to 0 (full snapshot).               |

## MeshChangesetRouteOptions

Options for the POST /mesh/changeset route handler.

| Property            | Type                       | Required | Description                                                       |
| ------------------- | -------------------------- | -------- | ----------------------------------------------------------------- |
| `adapter`           | `PostgresChangesetAdapter` | Yes      | PostgresChangesetAdapter instance wired to the PostgresBackend.   |
| `maxChangesetBytes` | `number \| undefined`      | No       | Maximum changeset size in bytes (default: 10 MB per P3 spec §10). |

## MeshChangesetResult

Generic POST /mesh/changeset handler result.  The route handler is framework-agnostic: the caller (Hono/Express/etc.) provides the request body and receives a structured result to render.

| Property | Type                                       | Required | Description                                                                |
| -------- | ------------------------------------------ | -------- | -------------------------------------------------------------------------- |
| `status` | `number`                                   | Yes      | HTTP status code to return.                                                |
| `body`   | `Record<string, unknown>`                  | Yes      | Response body as an object (caller serializes to JSON).                    |
| `delta`  | `Uint8Array<ArrayBufferLike> \| undefined` | No       | Delta changeset bytes to return in the response body (bidirectional sync). |

## A2AEnvelope

Canonical A2A message envelope (matches crates/llmtxt-core/src/a2a.rs).

| Property       | Type     | Required | Description                   |
| -------------- | -------- | -------- | ----------------------------- |
| `from`         | `string` | Yes      | from                          |
| `to`           | `string` | Yes      | to                            |
| `nonce`        | `string` | Yes      | nonce                         |
| `timestamp_ms` | `number` | Yes      | timestamp\_ms                 |
| `signature`    | `string` | Yes      | signature                     |
| `content_type` | `string` | Yes      | content\_type                 |
| `payload`      | `string` | Yes      | Base64-encoded payload bytes. |

## BuildA2AOptions

Options for building an A2A message.

| Property      | Type                  | Required | Description |
| ------------- | --------------------- | -------- | ----------- |
| `from`        | `string`              | Yes      | from        |
| `to`          | `string`              | Yes      | to          |
| `payload`     | `unknown`             | Yes      | payload     |
| `contentType` | `string \| undefined` | No       | contentType |
| `identity`    | `AgentIdentity`       | Yes      | identity    |
| `nowMs`       | `number \| undefined` | No       | nowMs       |
| `nonce`       | `string \| undefined` | No       | nonce       |

## InboxDeliveryResponse

Response from POST /agents/:id/inbox.

| Property       | Type      | Required | Description   |
| -------------- | --------- | -------- | ------------- |
| `delivered`    | `boolean` | Yes      | delivered     |
| `to`           | `string`  | Yes      | to            |
| `from`         | `string`  | Yes      | from          |
| `nonce`        | `string`  | Yes      | nonce         |
| `sig_verified` | `boolean` | Yes      | sig\_verified |
| `expires_at`   | `number`  | Yes      | expires\_at   |

## InboxMessage

Message in agent inbox response.

| Property      | Type          | Required | Description  |
| ------------- | ------------- | -------- | ------------ |
| `id`          | `string`      | Yes      | id           |
| `from`        | `string`      | Yes      | from         |
| `to`          | `string`      | Yes      | to           |
| `envelope`    | `A2AEnvelope` | Yes      | envelope     |
| `received_at` | `number`      | Yes      | received\_at |
| `expires_at`  | `number`      | Yes      | expires\_at  |
| `read`        | `boolean`     | Yes      | read         |

## InboxPollResponse

Response from GET /agents/:id/inbox.

| Property   | Type             | Required | Description |
| ---------- | ---------------- | -------- | ----------- |
| `messages` | `InboxMessage[]` | Yes      | messages    |
| `count`    | `number`         | Yes      | count       |

## BFTApprovalStatus

BFT approval status.

```typescript
BFTApprovalStatus
```

## SignedApprovalEnvelope

Signed approval envelope ready to POST to /bft/approve.

| Property            | Type                  | Required | Description        |
| ------------------- | --------------------- | -------- | ------------------ |
| `status`            | `BFTApprovalStatus`   | Yes      | status             |
| `sig_hex`           | `string`              | Yes      | sig\_hex           |
| `canonical_payload` | `string`              | Yes      | canonical\_payload |
| `comment`           | `string \| undefined` | No       | comment            |

## BFTApprovalResponse

Response from POST /documents/:slug/bft/approve.

| Property           | Type                | Required | Description      |
| ------------------ | ------------------- | -------- | ---------------- |
| `slug`             | `string`            | Yes      | slug             |
| `approvalId`       | `string`            | Yes      | approvalId       |
| `status`           | `BFTApprovalStatus` | Yes      | status           |
| `sigVerified`      | `boolean`           | Yes      | sigVerified      |
| `chainHash`        | `string`            | Yes      | chainHash        |
| `bftF`             | `number`            | Yes      | bftF             |
| `quorum`           | `number`            | Yes      | quorum           |
| `currentApprovals` | `number`            | Yes      | currentApprovals |
| `quorumReached`    | `boolean`           | Yes      | quorumReached    |

## BFTStatusResponse

Response from GET /documents/:slug/bft/status.

| Property           | Type       | Required | Description      |
| ------------------ | ---------- | -------- | ---------------- |
| `slug`             | `string`   | Yes      | slug             |
| `bftF`             | `number`   | Yes      | bftF             |
| `quorum`           | `number`   | Yes      | quorum           |
| `currentApprovals` | `number`   | Yes      | currentApprovals |
| `quorumReached`    | `boolean`  | Yes      | quorumReached    |
| `approvers`        | `string[]` | Yes      | approvers        |

## ChainVerificationResponse

Response from GET /documents/:slug/chain.

| Property         | Type             | Required | Description    |
| ---------------- | ---------------- | -------- | -------------- |
| `valid`          | `boolean`        | Yes      | valid          |
| `length`         | `number`         | Yes      | length         |
| `firstInvalidAt` | `number \| null` | Yes      | firstInvalidAt |
| `slug`           | `string`         | Yes      | slug           |

## ScratchpadMessage

A scratchpad message as returned by the API.

| Property       | Type                  | Required | Description   |
| -------------- | --------------------- | -------- | ------------- |
| `id`           | `string`              | Yes      | id            |
| `agent_id`     | `string`              | Yes      | agent\_id     |
| `content`      | `string`              | Yes      | content       |
| `content_type` | `string`              | Yes      | content\_type |
| `thread_id`    | `string \| undefined` | No       | thread\_id    |
| `sig_hex`      | `string \| undefined` | No       | sig\_hex      |
| `timestamp_ms` | `number`              | Yes      | timestamp\_ms |

## SendScratchpadOptions

Options for sending a scratchpad message.

| Property      | Type                         | Required | Description                                                         |
| ------------- | ---------------------------- | -------- | ------------------------------------------------------------------- |
| `content`     | `string`                     | Yes      | Message content body.                                               |
| `contentType` | `string \| undefined`        | No       | MIME content type (default: "text/plain").                          |
| `threadId`    | `string \| undefined`        | No       | Optional thread identifier for reply chains.                        |
| `identity`    | `AgentIdentity \| undefined` | No       | Agent identity for signing (optional — unsigned if omitted).        |
| `agentId`     | `string \| undefined`        | No       | Agent ID to include in the request (required if identity provided). |

## ReadScratchpadOptions

Options for reading scratchpad messages.

| Property   | Type                  | Required | Description                                        |
| ---------- | --------------------- | -------- | -------------------------------------------------- |
| `lastId`   | `string \| undefined` | No       | Return messages after this stream ID.              |
| `threadId` | `string \| undefined` | No       | Filter by thread.                                  |
| `limit`    | `number \| undefined` | No       | Maximum number of messages to return. Default 100. |
