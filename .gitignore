node_modules/
.env
*.db
*.db-journal
*.db-shm
*.db-wal
dist/
.DS_Store
*.log
textarea/
.vscode/
.idea/
docs/dev/
clawmsgr-agent.json
# Rust build artifacts
crates/*/target/
# WASM build output — the .gitignore inside wasm/ is managed by wasm-pack
# The wasm/ directory IS committed (needed for npm package consumers)
.gitnexus
.claude/worktrees/

# Rust build artifacts in examples
examples/audit-verifier/target/
examples/*/tarpaulin-report.json

# Tarpaulin coverage reports (generated artifacts)
crates/*/tarpaulin-report.json
tarpaulin-report.json

# legacy path (pre-2026-04-21) — agent outputs now live in .cleo/agent-outputs/
claudedocs/
